\documentclass[11pt]{article}
	
	%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	%\pdfminorversion=4
	% NOTE: To produce blinded version, replace "0" with "1" below.
	\newcommand{\blind}{0}
	
	%%%%%%% IISE Transactions margin specifications %%%%%%%%%%%%%%%%%%%
	% DON'T change margins - should be 1 inch all around.
	\addtolength{\oddsidemargin}{-.5in}%
	\addtolength{\evensidemargin}{-.5in}%
	\addtolength{\textwidth}{1in}%
	\addtolength{\textheight}{1.3in}%
	\addtolength{\topmargin}{-.8in}%
    \makeatletter
    \renewcommand\section{\@startsection {section}{1}{\z@}%
                                       {-3.5ex \@plus -1ex \@minus -.2ex}%
                                       {2.3ex \@plus.2ex}%
                                       {\normalfont\fontfamily{phv}\fontsize{16}{19}\bfseries}}
    \renewcommand\subsection{\@startsection{subsection}{2}{\z@}%
                                         {-3.25ex\@plus -1ex \@minus -.2ex}%
                                         {1.5ex \@plus .2ex}%
                                         {\normalfont\fontfamily{phv}\fontsize{14}{17}\bfseries}}
    \renewcommand\subsubsection{\@startsection{subsubsection}{3}{\z@}%
                                        {-3.25ex\@plus -1ex \@minus -.2ex}%
                                         {1.5ex \@plus .2ex}%
                                         {\normalfont\normalsize\fontfamily{phv}\fontsize{14}{17}\selectfont}}
    \makeatother
    %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	
	%%%%% IISE Transactions package list %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	\usepackage{amsmath}
	\usepackage[UTF8, heading=false]{ctex}
	\usepackage{graphicx}
	\usepackage{enumerate}
	\usepackage{natbib} %comment out if you do not have the package
	\usepackage{url} % not crucial - just used below for the URL
    \usepackage{xcolor}
    \usepackage{enumitem}
    \usepackage{amssymb}
    \usepackage{algorithm}
%    \floatstyle{plaintop} % 设置标题在顶部，且没有默认的粗横线
%    \restylefloat{algorithm}
    \usepackage{algpseudocode}
    \usepackage{bm}
    \usepackage{setspace}
	    \usepackage{float} % 需要这个包来控制样式
	    \usepackage{graphicx}
	    \usepackage{subcaption}
	    \usepackage{booktabs} % 用于专业表格线条 (\toprule, \midrule, \bottomrule)
	    \usepackage{multirow} % 用于合并单元格 (\multirow)
	    \captionsetup{font=small,skip=3pt}
	    \captionsetup[sub]{font=small,skip=2pt}
	    \setlength{\textfloatsep}{9pt plus 2pt minus 2pt}
	    \setlength{\floatsep}{8pt plus 2pt minus 2pt}
	    \setlength{\intextsep}{8pt plus 2pt minus 2pt}
	    \setlength{\abovecaptionskip}{3pt plus 1pt minus 1pt}
	    \setlength{\belowcaptionskip}{1pt plus 1pt minus 1pt}
	    \setcounter{topnumber}{5}
	    \setcounter{bottomnumber}{4}
	    \setcounter{totalnumber}{8}
	    \renewcommand{\topfraction}{0.95}
	    \renewcommand{\bottomfraction}{0.85}
	    \renewcommand{\textfraction}{0.05}
	    \renewcommand{\floatpagefraction}{0.78}
	    \emergencystretch=2em

		%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	
	%%%%% Author package list and commands %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	%%%%% Here are some examples %%%%%%%%%%%%%%
	%	\usepackage{amsfonts, amsthm, latexsym, amssymb}
	%	\usepackage{lineno}
	%	\newcommand{\mb}{\mathbf}
	%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
	
	\begin{document}
		
			%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
		\def\spacingset#1{\renewcommand{\baselinestretch}%
			{#1}\small\normalsize} \spacingset{1}
		%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
		
	\if0\blind
	{
		\title{Dual-Attention Deep Gaussian Processes for Multi-Objective Robust Parameter Design\\[0.5ex]\large 面向多目标鲁棒参数设计的双注意力深度高斯过程}
%		\author{Zebiao Feng $^a$, Xu Yang $^a$, Xiaojian Zhou $^a$ and Jianjun Wang $^b$\thanks{Corresponding author: Jianjun Wang (email: \texttt{jjwang@njust.edu.cn})} \\
%			$^a$ \parbox{1\textwidth}{School of Management, Nanjing University of Posts and Telecommunications, Nanjing 210003, China} \\
%			$^b$ \parbox{1\textwidth}{Department of Management Science and Engineering, Nanjing University of Science and Technology, Nanjing 210094, China}}
%		\date{}
		\maketitle
	} \fi
	
		
		\if1\blind
		{

            \title{Dual-Attention Deep Gaussian Processes for Multi-Objective Robust Parameter Design\\[0.5ex]\large 面向多目标鲁棒参数设计的双注意力深度高斯过程}
			\author{Zebiao Feng $^a$, Xu Yang $^a$, Xioajian Zhou $^a$ and Jianjun Wang $^b$\thanks{Corresponding author: Jianjun Wang (email: \texttt{jjwang@njust.edu.cn})} \\
				$^a$ \parbox{1\textwidth}{School of Management, Nanjing University of Posts and Telecommunications, Nanjing 210003, China} \\
				$^b$ \parbox{1\textwidth}{Department of Management Science and Engineering, Nanjing University of Science and Technology, Nanjing 210094, China}}
			
\bigskip
			\bigskip
			\bigskip
			\begin{center}
				{\LARGE\bf \emph{IISE Transactions} \LaTeX \ Template}
			\end{center}
			\medskip
		} \fi
		\bigskip
		
	\begin{abstract}
Robust parameter design (RPD) for modern complex engineering systems is frequently challenged by \textcolor{red}{limited engineering samples}% [#12]
, non-stationary response surfaces, and strongly conflicting objectives. Existing Gaussian process (GP) surrogate models, constrained by global stationarity assumptions and mean-oriented loss functions, often suffer from underfitting in the region of interest (ROI) critical for optimization. To overcome these limitations, this paper proposes a novel modeling framework named dual-attention deep Gaussian process (DA-DGP). A hierarchical attention mechanism is constructed: at the data level, a sample-level attention is designed to assign higher learning weights to samples within the ROI, achieving refined ROI-focused modeling of potential optimal regions; at the task level, a meta-learning-based dynamic weight adjustment strategy is introduced to effectively mitigate gradient conflicts among heterogeneous objectives. By deriving a rigorous weighted variational evidence lower bound (ELBO), the framework realizes end-to-end joint optimization of model parameters and attention weights. \textcolor{red}{Experiments on numerical benchmarks and a 5G/6G OFDM design show that, within the evaluated one-shot GP surrogate cohort, DA-DGP provides the most favorable overall evidence: it reduces average ROI RMSE by 14.3\%--59.0\% relative to the compared numerical baselines, achieves the top AHP-TOPSIS robust Pareto rank in the engineering case, and keeps the selected-solution EVM in the same high-quality range as sequentially augmented MOBO methods without requiring their iterative acquisition loop.}% [#9/#11/#12]

现代复杂工程系统的鲁棒参数设计（RPD）通常面临\textcolor{red}{有限工程样本}% [#12]
、非平稳响应曲面以及多目标强冲突等挑战。现有 Gaussian process（GP）代理模型受全局平稳性假设和均值导向损失函数限制，往往在对优化至关重要的 region of interest（ROI）内出现欠拟合。为克服这些局限，本文提出一种新的建模框架，即 dual-attention deep Gaussian process（DA-DGP）。该框架构建了分层注意力机制：在数据层面，设计样本级注意力，为 ROI 内样本分配更高学习权重，从而实现对潜在最优区域的 ROI 聚焦建模；在任务层面，引入基于 meta-learning 的动态权重调整策略，以有效缓解异质目标之间的梯度冲突。通过推导严格的加权变分 evidence lower bound（ELBO），该框架实现了模型参数与注意力权重的端到端联合优化。\textcolor{red}{数值基准与 5G/6G OFDM 设计实验表明，在所评估的一次性 GP 代理方法中，DA-DGP 给出了最有利的综合证据：在数值案例中，相比所比较的基线方法，其平均 ROI RMSE 降低 14.3\%--59.0\%；在工程案例中，其鲁棒 Pareto 前沿取得最高 AHP-TOPSIS 综合排名，且所选最优解的 EVM 与序贯式加点 MOBO 方法处于同一优良区间，同时不需要后者的迭代采集过程。}% [#9/#11/#12]
	\end{abstract}
			
	\noindent%
	{\it Keywords:} Robust parameter design; deep Gaussian processes; dual-attention mechanism; multi-objective optimization; computer experiments

	\noindent%
	{\it 关键词：} 鲁棒参数设计；deep Gaussian processes；双注意力机制；多目标优化；计算机实验

	%\newpage
	\spacingset{1.5} % DON'T change the spacing!


\section{Introduction} \label{s:intro}

Robust parameter design (RPD) serves as a cornerstone of modern industrial quality engineering. It aims to optimize controllable design factors to minimize the deviation of system output responses from target values while significantly reducing performance variations induced by uncontrollable noise factors \citep{Taguchi1986, Joseph2006, Montgomery2017, Hamada2021}. In advanced engineering applications such as aerospace, radar electronic countermeasures, and 5G/6G communication systems, product quality responses often exhibit highly nonlinear and multimodal characteristics \citep{Wang2020, Liu2025, Gnanasambandam2025, SongJ2025}. Taking orthogonal frequency division multiplexing (OFDM) as an example—a core physical layer technology in communication systems—despite its superior resistance to multipath fading, its practical deployment faces challenging multi-objective conflict problems. Specifically, increasing transmission power can improve the signal-to-noise ratio (SNR), thereby enhancing throughput and reducing the bit error rate (BER); however, this inevitably leads to a deterioration in the peak-to-average power ratio (PAPR) and a surge in energy consumption. This complex coupling relationship is governed by a series of intricate signal processing procedures, such as IFFT transformation, cyclic prefix insertion, and multipath channel convolution, resulting in high nonlinearity and typical “black-box” attributes. \textcolor{red}{Moreover, for a practical OFDM link, the same controllable design setting can produce different quality responses as stochastic bit streams, fading-channel realizations, and additive white Gaussian noise (AWGN) vary. We therefore treat these quantities as exogenous simulator noise: after surrogate-assisted optimization, candidate solutions are re-simulated under repeated realizations to estimate response means and cross-realization variances, so that noise propagation enters the robust assessment explicitly.} However, high-fidelity physical layer link simulations (involving steps such as random pilots and least squares channel estimation) are computationally expensive. A single link evaluation often requires millions of floating-point operations to resolve complex signal convolutions \citep{Song2025}. Furthermore, large-scale field testing is constrained by electromagnetic environments and prohibitive costs, leading to an extreme scarcity of high-quality samples available for modeling \citep{Ming2022, Sauer2022, Ghandwani2025}. Therefore, employing surrogate modeling to construct low-cost mathematical approximations of the real physical layer has become a key paradigm for addressing such optimization problems characterized by \textcolor{red}{limited engineering samples, explicit exogenous-noise propagation assessment,} and multiple conflicting objectives \citep{Luo2022, LiY2025}.

鲁棒参数设计（RPD）是现代工业质量工程的基石，其目标是优化可控设计因子，使系统输出响应相对于目标值的偏差最小化，同时显著降低不可控噪声因子引起的性能波动 \citep{Taguchi1986, Joseph2006, Montgomery2017, Hamada2021}。在航空航天、雷达电子对抗以及 5G/6G 通信系统等先进工程应用中，产品质量响应往往呈现高度非线性和多模态特征 \citep{Wang2020, Liu2025, Gnanasambandam2025, SongJ2025}。以通信系统核心物理层技术 orthogonal frequency division multiplexing（OFDM）为例，尽管其具有较强的抗多径衰落能力，但实际部署中仍面临棘手的多目标冲突问题。具体而言，提高发射功率可以改善 signal-to-noise ratio（SNR），从而提升吞吐量并降低 bit error rate（BER）；然而，这也不可避免地导致 peak-to-average power ratio（PAPR）恶化和能耗激增。这种复杂耦合关系由 IFFT 变换、循环前缀插入、多径信道卷积等一系列复杂信号处理过程共同决定，因此具有高度非线性和典型的“黑箱”属性。\textcolor{red}{此外，在实际 OFDM 链路中，同一组可控设计参数也会随随机比特流、衰落信道 realization 以及 additive white Gaussian noise（AWGN）的变化而产生不同质量响应。因此，本文将这些因素作为仿真器中的外生噪声源处理：在代理模型辅助优化得到候选解后，通过重复仿真估计响应均值及跨 realization 方差，使噪声传播效应显式进入鲁棒评价。}然而，高保真物理层链路仿真（涉及随机导频、最小二乘信道估计等步骤）计算成本高昂。单次链路评估通常需要数百万次浮点运算才能解析复杂信号卷积 \citep{Song2025}。此外，大规模外场测试受电磁环境和高昂成本制约，导致可用于建模的高质量样本极度稀缺 \citep{Ming2022, Sauer2022, Ghandwani2025}。因此，采用代理建模来构建真实物理层的低成本数学近似，已成为解决\textcolor{red}{有限工程样本、显式外生噪声传播评估与}多冲突目标优化问题的关键范式 \citep{Luo2022, LiY2025}。

Among various surrogate models, Gaussian processes (GP) have become the preferred tool for addressing such \textcolor{red}{sample-efficient engineering regression} problems due to their nonparametric flexibility and capability for uncertainty quantification (UQ) \citep{Williams2006, Deng2017, Joseph2019}. Unlike traditional parametric models, GPs utilize kernel functions to capture correlations among input variables, enabling the simultaneous prediction of response means and variances—a characteristic that inherently aligns with the ``mean-variance'' joint modeling requirement in robust parameter design \citep{Gramacy2020, Wang2025}. However, standard GPs typically rely on a single global stationary kernel, assuming uniform smoothness across the input space. This assumption limits their ability to capture multi-scale, abrupt, or non-stationary response surfaces \citep{Ko2022, Sauer2023, Tao2025}. Taking the OFDM communication system focused on in this paper as an example, influenced by the nonlinear coupling of carrier frequency offset drift and multipath effects, its BER response surface often exhibits drastic ``cliff-like'' abrupt changes, while the PAPR presents complex non-Gaussian distributions. Due to a lack of hierarchical feature extraction capabilities, single-layer (shallow) GPs struggle to accurately characterize such complex non-stationary interactions in high-dimensional spaces \citep{Zhai2022, Lin2024, WangX2025}. To overcome this limitation, deep Gaussian processes (DGP) have emerged \citep{Damianou2013}. By mapping the input space to a latent feature space through a multi-layer cascaded GP structure, DGPs possess strong capabilities for modeling non-stationary functions, demonstrating significant advantages in handling complex physical field simulations \citep{Muller2017, Ding2024}. 

在各种代理模型中，Gaussian processes（GP）凭借其非参数灵活性和 uncertainty quantification（UQ）能力，已成为处理此类\textcolor{red}{高样本效率工程回归}问题的首选工具 \citep{Williams2006, Deng2017, Joseph2019}。不同于传统参数模型，GP 利用核函数捕捉输入变量之间的相关性，能够同时预测响应均值与方差，这一特性天然契合鲁棒参数设计中“均值-方差”联合建模的需求 \citep{Gramacy2020, Wang2025}。然而，标准 GP 通常依赖单一全局平稳核，并假设输入空间各处具有一致平滑性。该假设限制了其捕捉多尺度、突变或非平稳响应曲面的能力 \citep{Ko2022, Sauer2023, Tao2025}。以本文关注的 OFDM 通信系统为例，受载波频偏漂移和多径效应非线性耦合影响，其 BER 响应曲面常呈现剧烈的“悬崖式”突变，而 PAPR 则表现出复杂的非 Gaussian 分布。由于缺乏层次化特征提取能力，单层（浅层）GP 难以准确刻画高维空间中的这类复杂非平稳交互 \citep{Zhai2022, Lin2024, WangX2025}。为突破这一局限，deep Gaussian processes（DGP）应运而生 \citep{Damianou2013}。DGP 通过多层级联 GP 结构将输入空间映射到潜在特征空间，具有较强的非平稳函数建模能力，在处理复杂物理场仿真时展现出显著优势 \citep{Muller2017, Ding2024}。

Nevertheless, existing DGP modeling frameworks still possess a core deficiency when dealing with robust parameter optimization tasks: their standard variational inference processes typically aim to maximize the global evidence lower bound (ELBO), essentially seeking to minimize the average fitting error across the entire input space. This ``averaging'' global loss function causes the model to tend towards smoothing global trends, often resulting in underfitting in the region of interest (ROI) that is critical for robust optimization \citep{Zhang2022}. \textcolor{red}{For multi-objective RPD, however, the most informative part of the surrogate is often the response geometry inside the ROI, where small prediction errors can change the estimated trade-off among responses.} Due to the lack of a mechanism to ``focus'' model capacity on key subspaces, limited computational resources are dispersed over low-performance regions irrelevant to optimization, severely limiting the potential for efficient robust optimization \textcolor{red}{under finite-sample engineering settings}.

然而，现有 DGP 建模框架在处理鲁棒参数优化任务时仍存在一个核心缺陷：其标准变分推断过程通常以最大化全局 evidence lower bound（ELBO）为目标，本质上是在整个输入空间内最小化平均拟合误差。这种“平均化”的全局损失函数会使模型倾向于平滑全局趋势，往往导致其在对鲁棒优化至关重要的 region of interest（ROI）内欠拟合 \citep{Zhang2022}。\textcolor{red}{而在多目标 RPD 中，真正影响设计决策的往往是 ROI 内的响应几何结构；该区域内较小的预测偏差，也可能改变多个响应之间的 trade-off 判断。}由于缺乏将模型容量“聚焦”到关键子空间的机制，有限计算资源被分散到与优化无关的低性能区域，严重限制了\textcolor{red}{有限工程样本条件下}高效鲁棒优化的潜力。

In light of the limitations inherent in traditional models that treat data indiscriminately, the attention mechanism—as a technique capable of dynamically filtering key information—has achieved significant success in deep learning and industrial intelligence in recent years \citep{Cho2024, Guo2025}. For instance, in fault diagnosis and remaining useful life (RUL) prediction tasks, models can effectively suppress redundant noise and enhance prediction accuracy by adaptively assigning weights to different feature channels or time steps \citep{Fan2025, Ma2025}. Inspired by these advancements, the academic community has recently begun attempting to introduce attention mechanisms into the Gaussian process domain: \cite{Schmidt2024} proposed a probabilistic attention method to improve deep multiple instance learning, while \cite{Nakamura2021} applied self-attention layers to sequence-to-sequence speech generation tasks. 
However, existing integration methods are mostly confined to single-layer attention structures or specific classification and sequence tasks, and have not yet systematically incorporated multi-level attention mechanisms into the hierarchical variational inference framework of DGPs \citep{Booth2025}.  Although recent studies have explored transfer learning strategies \citep{Yao2026} and multi-task structural learning \citep{Yang2025} to enhance modeling efficiency,  particularly in the field of RPD, how to leverage attention weights to simultaneously address the dual challenges of ``\textcolor{red}{sample-level emphasis on ROI-critical observations under limited data}'' and ``\textcolor{red}{task-level balancing of conflicting objectives and Pareto trade-offs}'' remains systematically unexplored.

鉴于传统模型对数据一视同仁的固有限制，attention mechanism 作为一种能够动态筛选关键信息的技术，近年来在 deep learning 和工业智能领域取得了显著成功 \citep{Cho2024, Guo2025}。例如，在故障诊断和 remaining useful life（RUL）预测任务中，模型可以通过自适应地为不同特征通道或时间步分配权重，有效抑制冗余噪声并提高预测精度 \citep{Fan2025, Ma2025}。受这些进展启发，学术界近期开始尝试将注意力机制引入 Gaussian process 领域：\cite{Schmidt2024} 提出了一种概率注意力方法以改进 deep multiple instance learning，而 \cite{Nakamura2021} 则将 self-attention 层应用于 sequence-to-sequence 语音生成任务。然而，现有融合方法大多局限于单层注意力结构或特定分类、序列任务，尚未系统地将多层级注意力机制纳入 DGP 的层次变分推断框架 \citep{Booth2025}。尽管近期研究已探索 transfer learning 策略 \citep{Yao2026} 和多任务结构学习 \citep{Yang2025} 以提升建模效率，但特别是在 RPD 领域，如何利用注意力权重同时应对“\textcolor{red}{有限数据下对 ROI 关键样本的样本级强调（Sample Selection）}”和“\textcolor{red}{面向冲突目标与 Pareto 权衡的任务级平衡（Task Balancing）}”这两类挑战，仍缺乏系统研究。

Driven by these needs, this paper proposes a novel framework named Dual-attention deep Gaussian process (DA-DGP). Unlike traditional multi-task Gaussian processes that rely on explicit task covariance matrices (e.g., the linear model of coregionalization, LMC), the proposed model utilizes deep structures to implicitly capture complex dependencies among outputs. This framework is specifically tailored for RPD problems characterized by \textcolor{red}{limited engineering samples}, high-dimensional nonlinearities, and strongly conflicting objectives. The core philosophy is to synergize two attention mechanisms with varying granularities to intelligently guide and allocate limited modeling resources. The main contributions of this paper are summarized as follows:

在上述需求驱动下，本文提出一种新的框架，称为 Dual-attention deep Gaussian process（DA-DGP）。不同于依赖显式任务协方差矩阵的传统 multi-task Gaussian processes（例如 linear model of coregionalization，LMC），所提出模型利用深层结构隐式捕捉输出之间的复杂依赖关系。该框架专门面向\textcolor{red}{有限工程样本}、高维非线性和强目标冲突特征明显的 RPD 问题。其核心思想是协同不同粒度的两类注意力机制，以智能引导并分配有限建模资源。本文主要贡献总结如下：

\begin{enumerate}[label=(\arabic*), wide, labelwidth=!, labelindent=0pt]
	\item \textbf{Sample-level Attention:} To address the ROI-driven optimization characteristics of RPD, \textcolor{red}{target-oriented Gaussian sample weights are introduced into the variational data-fitting term rather than into the kernel itself}. This mechanism guides the model to focus on the target ROI under \textcolor{red}{limited training samples}, improving prediction accuracy in critical subspaces.

	\textbf{样本级注意力：} 针对 RPD 的 ROI 驱动优化特征，\textcolor{red}{将目标导向的 Gaussian 样本权重引入变分数据拟合项，而不是直接修改核函数本身}。该机制引导模型在\textcolor{red}{有限训练样本条件下}聚焦目标 ROI，从而提升关键子空间内的预测精度。

	\item \textbf{Task-level Attention:} To address the strong conflicts among multiple objectives, a bilevel optimization strategy based on meta-learning is designed. This mechanism adaptively learns the optimal weights for conflicting tasks based on validation set performance, overcoming the limitations of traditional static weighting methods.

	\textbf{任务级注意力：} 针对多个目标之间的强冲突，设计了基于 meta-learning 的双层优化策略。该机制根据验证集性能自适应学习冲突任务的最优权重，克服传统静态加权方法的局限。

	\item \textbf{Weighted Variational Inference:} A rigorous weighted variational ELBO is derived, seamlessly integrating the aforementioned dual-attention mechanisms into the end-to-end training of the DGP. This realizes the collaborative optimization of model parameters and attention weights.

	\textbf{加权变分推断：} 推导了严格的加权变分 ELBO，将上述双注意力机制无缝融入 DGP 的端到端训练过程，实现模型参数与注意力权重的协同优化。

	\item \textbf{Engineering Validation and Evaluation:} Through standard numerical benchmark functions and \textcolor{red}{a high-dimensional 5G/6G OFDM communication link optimization case with explicit noise realizations}, the advantages and boundaries of the proposed method in handling non-stationary responses, multi-objective conflicts, and \textcolor{red}{robust Pareto trade-offs are systematically evaluated against strong Gaussian process surrogate baselines, ablation variants, sensitivity analyses, and representative BO methods}.

	\textbf{工程验证与评估：} 通过标准数值基准函数，以及\textcolor{red}{具有显式噪声 realization 的高维 5G/6G OFDM 通信链路优化案例}，系统评估所提方法在处理非平稳响应、多目标冲突和\textcolor{red}{鲁棒 Pareto 权衡方面的优势与边界，并与强 GP 专用基线、消融变体、敏感性分析以及代表性 BO 方法进行比较}。
\end{enumerate}

The remainder of this paper is organized as follows: Section 2 briefly reviews the theoretical foundations of GPs and DGPs, elucidating the applicability of multi-output DGPs in handling high-dimensional non-stationary responses. Section 3 details the proposed DA-DGP framework, focusing on the construction of the sample-level attention for precisely locating the ROI and the task-level attention mechanism for balancing multi-objective conflicts; furthermore, the weighted ELBO and the multi-objective robust optimization model are derived. Section 4 \textcolor{red}{presents numerical and engineering validation, including strong Gaussian process surrogate baselines, ablation studies, sensitivity analyses, and robust Pareto/BO comparisons}. Finally, Section 5 concludes the paper and outlines future research directions.

本文其余部分安排如下：第 2 节简要回顾 GPs 和 DGPs 的理论基础，阐明 multi-output DGPs 在处理高维非平稳响应方面的适用性。第 3 节详细介绍所提出的 DA-DGP 框架，重点讨论用于精准定位 ROI 的样本级注意力构建，以及用于平衡多目标冲突的任务级注意力机制；此外，还推导加权 ELBO 和多目标鲁棒优化模型。第 4 节\textcolor{red}{给出数值案例与工程案例验证，包括强 GP 专用基线、消融实验、敏感性分析以及鲁棒 Pareto/BO 比较}。最后，第 5 节总结全文并展望未来研究方向。

\section{Theoretical Backgrounds} \label{sec:background}

Let $\mathbf{x} \in \mathbb{R}^d$ denote the input design factors and $y \in \mathbb{R}$ be the system response. We aim to construct a surrogate model based on a dataset $\mathcal{D}=\{\mathbf{X}, \mathbf{y}\}$ containing $N$ observations, assuming a noisy relationship $y = f(\mathbf{x}) + \epsilon$ with Gaussian noise $\epsilon \sim \mathcal{N}(0, \sigma_n^2)$.

令 $\mathbf{x} \in \mathbb{R}^d$ 表示输入设计因子，$y \in \mathbb{R}$ 表示系统响应。本文旨在基于包含 $N$ 个观测值的数据集 $\mathcal{D}=\{\mathbf{X}, \mathbf{y}\}$ 构建代理模型，并假设二者满足含噪关系 $y = f(\mathbf{x}) + \epsilon$，其中 Gaussian noise 为 $\epsilon \sim \mathcal{N}(0, \sigma_n^2)$。

\subsection{Standard Gaussian Processes} \label{subsec:gp}

A Gaussian Process (GP) places a prior distribution over the latent function $f$, assuming that any finite collection of function values has a joint Gaussian distribution. Formally, the GP prior is specified by a mean function $m(\mathbf{x})$ (assumed zero for simplicity) and a covariance function $k(\mathbf{x}, \mathbf{x}')$:

Gaussian Process（GP）为潜在函数 $f$ 赋予先验分布，并假设任意有限个函数值服从联合 Gaussian 分布。形式上，GP 先验由均值函数 $m(\mathbf{x})$（为简化起见假设为零）和协方差函数 $k(\mathbf{x}, \mathbf{x}')$ 共同刻画：
\begin{equation}
	f(\mathbf{x}) \sim \mathcal{GP}(0, k(\mathbf{x}, \mathbf{x}')).
\end{equation}
To capture the non-linear correlations with different sensitivities across input dimensions, \textcolor{red}{both case studies and all Gaussian process surrogate baselines use the anisotropic Mat\'ern-5/2 kernel with Automatic Relevance Determination (ARD):}

为捕捉输入维度间具有不同敏感性的非线性相关关系，\textcolor{red}{两个案例及全部高斯过程代理基线均采用带有 Automatic Relevance Determination（ARD）的各向异性 Mat\'ern-5/2 核：}
\begin{equation}
	k(\mathbf{x}, \mathbf{x}') = \sigma_f^2 \textcolor{red}{\left( 1 + \sqrt{5}r(\mathbf{x}, \mathbf{x}') + \frac{5}{3}r^2(\mathbf{x}, \mathbf{x}') \right)\exp \left( - \sqrt{5}r(\mathbf{x}, \mathbf{x}') \right)} + \sigma_n^2 \delta_{ij},
\end{equation}
where $\sigma_f^2$ is the signal variance, $\mathbf{L} = \text{diag}(l_1^2, \ldots, l_d^2)$ contains the characteristic length-scales for each dimension, \textcolor{red}{$r(\mathbf{x}, \mathbf{x}') = \sqrt{(\mathbf{x} - \mathbf{x}')^T \mathbf{L}^{-1} (\mathbf{x} - \mathbf{x}')}$}, and $\delta_{ij}$ is the Kronecker delta function.

其中，$\sigma_f^2$ 为信号方差，$\mathbf{L} = \text{diag}(l_1^2, \ldots, l_d^2)$ 包含各维度的特征长度尺度，\textcolor{red}{$r(\mathbf{x}, \mathbf{x}') = \sqrt{(\mathbf{x} - \mathbf{x}')^T \mathbf{L}^{-1} (\mathbf{x} - \mathbf{x}')}$}，$\delta_{ij}$ 为 Kronecker delta function。

Conditioned on the dataset $\mathcal{D}$, the posterior predictive distribution at a new test point $\mathbf{x}_*$ follows a Gaussian distribution $p(f_* | \mathbf{X}, \mathbf{y}, \mathbf{x}_*) \sim \mathcal{N}(\mu_*(\mathbf{x}_*), \sigma_*^2(\mathbf{x}_*))$, with moments given by:

在数据集 $\mathcal{D}$ 条件下，新测试点 $\mathbf{x}_*$ 处的后验预测分布服从 Gaussian 分布 $p(f_* | \mathbf{X}, \mathbf{y}, \mathbf{x}_*) \sim \mathcal{N}(\mu_*(\mathbf{x}_*), \sigma_*^2(\mathbf{x}_*))$，其矩如下：
\begin{equation}
	\begin{aligned}
		\mu_*(\mathbf{x}_*) &= \mathbf{k}_{*N} (\mathbf{K}_{NN} + \sigma_n^2 \mathbf{I})^{-1} \mathbf{y}, \\
		\sigma_*^2(\mathbf{x}_*) &= k(\mathbf{x}_*, \mathbf{x}_*) - \mathbf{k}_{*N} (\mathbf{K}_{NN} + \sigma_n^2 \mathbf{I})^{-1} \mathbf{k}_{N*} + \sigma_n^2,
	\end{aligned}
\end{equation}
where $\mathbf{K}_{NN} = k(\mathbf{X}, \mathbf{X})$ is the covariance matrix of training data and $\mathbf{k}_{*N} = k(\mathbf{x}_*, \mathbf{X})$. The computational complexity is dominated by the inversion of $(\mathbf{K}_{NN} + \sigma_n^2 \mathbf{I})$, scaling as $\mathcal{O}(N^3)$.

其中，$\mathbf{K}_{NN} = k(\mathbf{X}, \mathbf{X})$ 为训练数据的协方差矩阵，$\mathbf{k}_{*N} = k(\mathbf{x}_*, \mathbf{X})$。计算复杂度主要由 $(\mathbf{K}_{NN} + \sigma_n^2 \mathbf{I})$ 的矩阵求逆主导，其量级为 $\mathcal{O}(N^3)$。



\subsection{Sparse Variational Approximation} \label{s:sec2.2}

To address the cubic computational bottleneck, sparse approximation techniques utilize a set of $M$ inducing points $\mathbf{Z} = [\mathbf{z}_1, \ldots, \mathbf{z}_M]^T$ with $M \ll N$, and corresponding inducing variables $\mathbf{u} = f(\mathbf{Z})$. Following the variational inference framework \citep{Titsias2009}, the intractable true posterior is approximated by a variational distribution $q(\mathbf{u}) = \mathcal{N}(\mathbf{m}, \mathbf{S})$. 

为解决三次计算复杂度瓶颈，稀疏近似技术使用一组 $M$ 个 inducing points $\mathbf{Z} = [\mathbf{z}_1, \ldots, \mathbf{z}_M]^T$（$M \ll N$）及其对应的 inducing variables $\mathbf{u} = f(\mathbf{Z})$。遵循 variational inference 框架 \citep{Titsias2009}，难以直接求解的真实后验由变分分布 $q(\mathbf{u}) = \mathcal{N}(\mathbf{m}, \mathbf{S})$ 近似。

By assuming conditional independence between training points given $\mathbf{u}$, the joint variational posterior factorizes as $q(\mathbf{f}, \mathbf{u}) = p(\mathbf{f}|\mathbf{u})q(\mathbf{u})$. The model parameters and variational parameters are jointly optimized by maximizing the Evidence Lower Bound (ELBO):

在给定 $\mathbf{u}$ 时假设训练点之间条件独立，联合变分后验可分解为 $q(\mathbf{f}, \mathbf{u}) = p(\mathbf{f}|\mathbf{u})q(\mathbf{u})$。模型参数和变分参数通过最大化 Evidence Lower Bound（ELBO）进行联合优化：
\begin{equation}
	\mathcal{L}_{\text{SGP}} = \sum_{i=1}^{N} \mathbb{E}_{q(f_i)} [\log p(y_i | f_i)] - \text{KL}[q(\mathbf{u}) || p(\mathbf{u})],
\end{equation}
where the first term represents the expected log-likelihood (model fit) and the second term is the Kullback-Leibler divergence between the variational posterior and the prior (regularization). The detailed derivation of this factorization and the subsequent ELBO formulation is provided in Appendix A. This formulation reduces the computational complexity to $\mathcal{O}(NM^2)$ and supports mini-batch training, providing a scalable foundation for the deep Gaussian processes discussed in the subsequent sections.

其中，第一项表示期望 log-likelihood（模型拟合），第二项为变分后验与先验之间的 Kullback-Leibler divergence（正则化）。该分解及后续 ELBO 形式的详细推导见 Appendix A。该形式将计算复杂度降低到 $\mathcal{O}(NM^2)$，并支持 mini-batch 训练，为后续章节讨论的 deep Gaussian processes 提供可扩展基础。


\subsection{Multi-output Deep Gaussian Process Framework} \label{s:sec2.3}
To overcome the limitations of stationary kernels in single-layer GPs, the DGP framework is employed \cite{Li2025}. This architecture hierarchically stacks sparse GP modules to capture complex non-stationary features while preserving Bayesian uncertainty. For the multi-objective nature of RPD, a multi-output structure is constructed (Figure \ref{fig:1}). \textcolor{red}{In the experiments of this paper, the default surrogate adopts two stochastic GP layers (one hidden layer and one output layer), while deeper variants are examined later only for sensitivity analysis rather than as the default architecture.}

为克服单层 GP 中平稳核的局限，本文采用 DGP 框架 \cite{Li2025}。该结构以层次化方式堆叠 sparse GP 模块，在保留 Bayesian uncertainty 的同时捕捉复杂非平稳特征。针对 RPD 的多目标性质，本文构建了 multi-output 结构（Figure \ref{fig:1}）。\textcolor{red}{在本文实验中，默认代理模型采用两层随机 GP 结构（一个隐藏层加一个输出层），而更深的结构仅在后文作为敏感性分析对象出现，并不作为默认架构。}

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.9\linewidth]{FIG1.pdf}
	\caption{Architecture of the multi-output DGP. Connections represent probabilistic conditional dependencies rather than deterministic weights.}
	\caption*{多输出 DGP 的结构。连接表示概率条件依赖关系，而非确定性权重。}
	\label{fig:1}
\end{figure}

The generative process of an $L$-layer DGP is defined as a composition of functions $f^l(\cdot)$:

$L$ 层 DGP 的生成过程被定义为函数 $f^l(\cdot)$ 的复合：
\begin{equation}
	{{\bf{F}}^0} = {\bf{X}}{\rm{ }},
\end{equation}
\begin{equation}
	{{\mathbf{F}}^{l}}\sim\mathcal{G}\mathcal{P}({{m}_{l}}({{\mathbf{F}}^{l-1}}),{{k}_{l}}({{\mathbf{F}}^{l-1}},{{\mathbf{F}}^{l-1}})),\quad l=1,\ldots ,L,
\end{equation}
where $\mathbf{F}^l$ denotes the matrix of function values at the $l$-th layer. The output layer of the model contains $p$ independent GP nodes, corresponding to the $p$ quality responses to be optimized. The observed data matrix $\mathbf{Y}$ is generated via the likelihood function $p(\mathbf{Y}|\mathbf{F}^L)$. To ensure computational feasibility, the concept of Sparse GPs (SGPs) is introduced at each layer, defining a set of inducing points $\mathbf{Z}^l$ and corresponding inducing variables $\mathbf{U}^l$. Consequently, the joint probability distribution of the DGP can be expressed as:

其中，$\mathbf{F}^l$ 表示第 $l$ 层的函数值矩阵。模型输出层包含 $p$ 个相互独立的 GP 节点，对应待优化的 $p$ 个质量响应。观测数据矩阵 $\mathbf{Y}$ 由 likelihood function $p(\mathbf{Y}|\mathbf{F}^L)$ 生成。为保证计算可行性，每一层都引入 Sparse GPs（SGPs）的概念，定义一组 inducing points $\mathbf{Z}^l$ 及对应的 inducing variables $\mathbf{U}^l$。因此，DGP 的联合概率分布可表示为：
\begin{equation}
	p({\bf{Y}},\{ {{\bf{F}}^l},{{\bf{U}}^l}\} _{l = 1}^L) = p({\bf{Y}}|{{\bf{F}}^L})\prod\limits_{l = 1}^L p ({{\bf{F}}^l}|{{\bf{U}}^l},{{\bf{F}}^{l - 1}},{{\bf{Z}}^{l - 1}})p({{\bf{U}}^l};{{\bf{Z}}^{l - 1}}).
\end{equation}

Unlike the Linear Model of Coregionalization (LMC), this deep architecture implicitly captures nonlinear task correlations via shared latent representations. \textcolor{red}{This shared-latent construction should also be distinguished from independent GP/DGP surrogates: LMC imposes explicit coregionalization at the output level, whereas independent surrogates remove parameter sharing across tasks altogether.} Since exact inference is intractable, variational inference is utilized. A layer-wise factorized variational posterior is specified to maintain the Markov property:

不同于 Linear Model of Coregionalization（LMC），该深层结构通过共享潜在表征隐式捕捉非线性任务相关性。\textcolor{red}{这种共享潜在表示的构造还需要与独立 GP/DGP 代理加以区分：LMC 在输出层施加显式 coregionalization，而独立代理则完全取消任务间参数共享。}由于精确推断不可解，本文采用 variational inference。为保持 Markov property，指定逐层分解的变分后验：
\begin{equation}
	q(\{ {{\bf{F}}^l},{{\bf{U}}^l}\} _{l = 1}^L) = \prod\limits_{l = 1}^L p ({{\bf{F}}^l}|{{\bf{U}}^l},{{\bf{F}}^{l - 1}},{{\bf{Z}}^{l - 1}})q({{\bf{U}}^l}){\rm{ }},
\end{equation}
where $q(\mathbf{U}^l)$ is the variational distribution of the inducing variables at the $l$-th layer. Based on this variational posterior, the ELBO for the multi-output DGP can be derived as:

其中，$q(\mathbf{U}^l)$ 为第 $l$ 层 inducing variables 的变分分布。基于该变分后验，multi-output DGP 的 ELBO 可推导为：
\begin{equation}
	{{\mathcal{L}}_{\text{DGP}}}=\sum\limits_{k=1}^{p}{\sum\limits_{i=1}^{N}{{{\mathbb{E}}_{q(f_{i,k}^{L})}}}}[\log p({{y}_{i,k}}|f_{i,k}^{L})]-\sum\limits_{l=1}^{L}{\text{KL}}[q({{\mathbf{U}}^{l}})||p({{\mathbf{U}}^{l}};{{\mathbf{Z}}^{l-1}})].
\end{equation}

The expectation term in Equation (9) is analytically intractable due to the complex distributions induced by multiple nonlinear stochastic layers. Consequently, the Doubly Stochastic Variational Inference (DSVI) strategy \cite{Salimbeni2017} is adopted. Monte Carlo sampling is performed via the reparameterization trick: letting $\hat{\mathbf{f}}_i^0 = \mathbf{x}_i$ denote the input, samples are recursively generated for $l=1, \dots, L$ by first sampling a standard Gaussian noise vector:

由于多层非线性随机层会诱导复杂分布，Equation (9) 中的期望项难以解析求解。因此，本文采用 Doubly Stochastic Variational Inference（DSVI）策略 \cite{Salimbeni2017}。Monte Carlo sampling 通过 reparameterization trick 实现：令 $\hat{\mathbf{f}}_i^0 = \mathbf{x}_i$ 表示输入，首先采样一个标准 Gaussian 噪声向量，并对 $l=1, \dots, L$ 递归生成样本：
\begin{equation}
	\epsilon _{i}^{l}\sim\mathcal{N}(\mathbf{0},{{\mathbf{I}}_{{{d}_{l}}}}).
\end{equation}
Then, the function values are updated via a deterministic transformation:

随后，函数值通过确定性变换更新：
\begin{equation}
	\widehat{\mathbf{f}}_{i}^{l}={{\mathbf{\mu }}_{l}}(\widehat{\mathbf{f}}_{i}^{l-1})+\epsilon _{i}^{l}\odot \sqrt{\text{va}{{\text{r}}_{l}}(\widehat{\mathbf{f}}_{i}^{l-1})},
\end{equation}
where $\odot$ denotes the Hadamard product. This transformation allows gradients to backpropagate through the stochastic sampling step (proof in Appendix B). Combined with data subsampling, this constructs an unbiased ELBO estimator, enabling efficient end-to-end training via Mini-batch SGD.
However, the standard ELBO (9) represents an ``equal-weighted summation'' of reconstruction errors, effectively minimizing the global average error. This strategy fails to account for the conflicting nature of RPD objectives and the heterogeneous importance of samples for locating the optimal solution. To prioritize the ROI and critical responses, a weighted variational objective is formulated by introducing weights to the data fitting term:

其中，$\odot$ 表示 Hadamard product。该变换允许梯度通过随机采样步骤反向传播（证明见 Appendix B）。结合数据子采样后，可构造无偏 ELBO 估计器，从而通过 Mini-batch SGD 实现高效端到端训练。然而，标准 ELBO (9) 表示重构误差的“等权求和”，实际上是在最小化全局平均误差。该策略无法考虑 RPD 目标之间的冲突性，也无法体现不同样本在定位最优解时的重要性差异。为优先关注 ROI 和关键响应，本文通过在数据拟合项中引入权重来构造加权变分目标：
\begin{equation}
	{{\mathcal{L}}_{Weighted}}=\sum\limits_{k=1}^{p}{\sum\limits_{i=1}^{N}{{{{\tilde{w}}}_{i,k}}}}{{\mathbb{E}}_{q(\mathbf{f}_{i,k}^{L})}}[\log p({{y}_{i,k}}|\mathbf{f}_{i,k}^{L})]-\sum\limits_{l=1}^{L}{\text{KL}}(q({{\mathbf{U}}^{l}})||p({{\mathbf{U}}^{l}})),
\end{equation}
where $\tilde{w}_{i,k}$ represents the composite weight for the $i$-th sample in the $k$-th task (generated by the dual-attention mechanism described later). This objective function can be interpreted as a criterion for ``preference-weighted" risk minimization, aiming to guide the model to allocate more learning capacity to the ROI and critical tasks. Obviously, when $\tilde{w}_{i,k}=1$, this objective degenerates to the standard ELBO.

其中，$\tilde{w}_{i,k}$ 表示第 $k$ 个任务中第 $i$ 个样本的复合权重（由后文的双注意力机制生成）。该目标函数可解释为一种“偏好加权”风险最小化准则，旨在引导模型向 ROI 和关键任务分配更多学习容量。显然，当 $\tilde{w}_{i,k}=1$ 时，该目标退化为标准 ELBO。


\section{DA-DGP Modeling and Optimization} \label{s:sec3}
To synergize ROI-focused refinement with global task balancing, a dual-layer multi-attention framework is proposed. This architecture hierarchically integrates static target-oriented sample attention to prioritize the ROI and dynamic meta-learning-driven task weighting to resolve objective conflicts. Through this design, ROI-oriented exploitation and macroscopic coordination are unified to efficiently approximate the Pareto frontier.

为协同 ROI 聚焦精化与全局任务平衡，本文提出双层 multi-attention 框架。该结构层次化整合静态目标导向样本注意力以优先关注 ROI，并结合动态 meta-learning 驱动的任务加权以解决目标冲突。通过该设计，面向 ROI 的利用与宏观层面的整体协调得以统一，从而高效逼近 Pareto frontier。

\subsection{Target-Oriented Sample-Level Attention} \label{s:sec3.1}
The standard ELBO (Eq. 9) employs an equal-weight summation strategy, which inevitably dilutes fitting signals from the ROI critical for RPD. To mitigate this, a soft sample-level attention mechanism is introduced as a differentiable feature filter. By reconstructing the likelihood term, this mechanism compels the model to prioritize data subsets proximal to potential optimal solutions, thereby enhancing ROI prediction accuracy.

标准 ELBO（Eq. 9）采用等权求和策略，这不可避免地稀释了来自 RPD 关键 ROI 的拟合信号。为缓解这一问题，本文引入一种软样本级注意力机制作为可微特征过滤器。通过重构 likelihood 项，该机制促使模型优先关注接近潜在最优解的数据子集，从而提高 ROI 预测精度。

Specifically, consider a multi-task learning problem with $p$ response variables, where $y_{k}^{*}$ denotes the ideal target value predefined for the $k$-th task ($k \in \{1, \ldots, p\}$). Given the training dataset $\mathcal{D}=\{(\mathbf{x}_{i}, \mathbf{y}_{i})\}_{i=1}^{N}$ with observation vectors $\mathbf{y}_{i}=[y_{i,1}, \ldots, y_{i,p}]^{T}$, we quantify the relevance of sample $i$ to task $k$ using a Gaussian Radial Basis Function (RBF) kernel. This maps the Euclidean distance between the observation and the target to an attention score in $(0, 1]$. Consequently, the raw attention weight $s_{i,k}$ is defined as:

具体而言，考虑一个具有 $p$ 个响应变量的 multi-task learning 问题，其中 $y_{k}^{*}$ 表示第 $k$ 个任务（$k \in \{1, \ldots, p\}$）预先设定的理想目标值。给定训练数据集 $\mathcal{D}=\{(\mathbf{x}_{i}, \mathbf{y}_{i})\}_{i=1}^{N}$，其观测向量为 $\mathbf{y}_{i}=[y_{i,1}, \ldots, y_{i,p}]^{T}$，本文使用 Gaussian Radial Basis Function（RBF）kernel 来量化样本 $i$ 与任务 $k$ 的相关性。该方法将观测值与目标值之间的 Euclidean distance 映射为 $(0, 1]$ 内的注意力得分。因此，原始注意力权重 $s_{i,k}$ 定义为：
\begin{equation}
	{{s}_{i,k}}=\exp \left( -\frac{\|{{y}_{i,k}}-y_{k}^{*}{{\|}^{2}}}{2\sigma _{k}^{2}} \right),
\end{equation}
where $\|\cdot\|$ denotes the Euclidean norm and $\sigma_{k}$ controls the kernel bandwidth. This parameter modulates the attention decay rate: a smaller $\sigma_{k}$ induces a sharp focus on the immediate target vicinity, whereas a larger value expands the receptive field for broader coverage.

其中，$\|\cdot\|$ 表示 Euclidean norm，$\sigma_{k}$ 控制 kernel bandwidth。该参数调节注意力衰减速率：较小的 $\sigma_{k}$ 会使模型尖锐聚焦于目标附近区域，而较大的取值则会扩大感受范围以获得更广覆盖。

In practice, hyperparameters are specified explicitly for each case rather than black-box optimized. The target $y_{k}^{*}$ is directly derived from design specifications or ROI preferences. \textcolor{red}{When outputs are standardized before training, the configured target values and bandwidths are mapped to the standardized output space before attention weights are computed. When the engineering target is not uniquely prescribed, neighboring candidate targets can be examined through sensitivity analysis to assess the robustness of ROI localization.} The bandwidth $\sigma_{k}$ \textcolor{red}{is configured per case and kept fixed during training rather than jointly learned}. \textcolor{red}{This transparent specification supports reproducible ROI definition; treating $\sigma_{k}$ as a learnable parameter is left for future work, while the numerical sensitivity study later in the paper shows that fixed choices remain stable over a practically useful range.} To maintain numerical stability and scale consistency with the standard ELBO, the raw weights are normalized:

在实践中，超参数按案例显式设定，而非依赖黑箱优化。目标 $y_{k}^{*}$ 直接由设计规范或 ROI 偏好给出。\textcolor{red}{当训练前对输出进行了标准化时，所配置的目标值和带宽会先映射到标准化输出空间，再据此计算注意力权重。当工程目标并非唯一给定时，还可以通过邻近候选目标的敏感性分析来评估 ROI 定位的稳健性。}带宽 $\sigma_{k}$ \textcolor{red}{按案例配置并在训练过程中保持固定，而不是联合学习}。\textcolor{red}{这种透明设定有助于保证 ROI 定义的可复现性；将 $\sigma_{k}$ 设为可学习参数可作为未来工作，而本文后续数值敏感性分析表明固定设定在实际可用范围内仍具有较好的稳定性。}为保持数值稳定性并与标准 ELBO 的尺度一致，本文对原始权重进行归一化：
\begin{equation}
	{{\tilde{s}}_{i,k}}=\frac{{{s}_{i,k}}}{\sum\limits_{j=1}^{N}{{{s}_{j,k}}}}\cdot N  \qquad .
\end{equation}
This strategy ensures that $\sum_{i=1}^{N} \tilde{s}_{i,k} = N$. Based on these normalized weights, the expected log-likelihood term in Equation (9) is reconstructed. For task $k$, the sample-weighted loss function $\mathcal{L}'_{k}(\boldsymbol{\theta})$ can be expressed as:

该策略保证 $\sum_{i=1}^{N} \tilde{s}_{i,k} = N$。基于这些归一化权重，Equation (9) 中的期望 log-likelihood 项被重构。对于任务 $k$，样本加权损失函数 $\mathcal{L}'_{k}(\boldsymbol{\theta})$ 可表示为：
\begin{equation}
	{{{\mathcal{L}}'}_{k}}(\boldsymbol{\theta })=\frac{1}{N}\sum\limits_{i=1}^{N}{{{{\tilde{s}}}_{i,k}}}\cdot {{\ell }_{k}}({{f}_{\boldsymbol{\theta }}}({{\mathbf{x}}_{i}}),{{y}_{i,k}}),
\end{equation}
where $\ell_{k}(\cdot)$ corresponds to the negative log-likelihood $-\log p(y_{i,k}|f_{i,k}^{L})$ in the first term of Equation (9). Integrating the normalized weights (15) into the standard variational framework (9) yields a preference-aware ELBO. The theoretical justification for this weighted objective, based on generalized variational inference, is provided in Appendix C. Consequently, the updated optimization objective $\mathcal{L}_{DGP}$ for $p$ tasks is formulated as:

其中，$\ell_{k}(\cdot)$ 对应 Equation (9) 第一项中的 negative log-likelihood $-\log p(y_{i,k}|f_{i,k}^{L})$。将归一化权重 (15) 融入标准变分框架 (9)，可得到 preference-aware ELBO。该加权目标基于 generalized variational inference 的理论依据见 Appendix C。因此，$p$ 个任务的更新优化目标 $\mathcal{L}_{DGP}$ 可写为：
\begin{equation}
	{{\mathcal{L}}_{DGP}}=\sum\limits_{k=1}^{p}{\left( \frac{1}{N}\sum\limits_{i=1}^{N}{{{{\tilde{s}}}_{i,k}}}\cdot \mathbb{E}\left[ \log p({{y}_{i,k}}|f_{i,k}^{L}) \right] \right)}-\sum\limits_{l=1}^{L}{\text{KL}}\left( q({{\mathbf{U}}^{l}})\|p({{\mathbf{U}}^{l}}) \right),
\end{equation}
where the first term utilizes normalized weights $\tilde{s}_{i,k}$ to bias the expected log-likelihood toward the Region of Interest (ROI), thereby dominating gradient updates; the second term imposes standard KL divergence regularization to prevent overfitting.

其中，第一项利用归一化权重 $\tilde{s}_{i,k}$ 将期望 log-likelihood 偏向 Region of Interest（ROI），从而主导梯度更新；第二项施加标准 KL divergence 正则化以防止过拟合。

Statistically, Equation (16) implements a focused weighted likelihood estimation. The weights $\tilde{s}_{i,k}$ modulate the contribution of each observation to the posterior inference based on target proximity (Figure \ref{fig:2}). By amplifying update signals from critical regions while attenuating interference from irrelevant data fluctuations, this mechanism adaptively reallocates fitting resources to the ROI. Unlike hard-thresholding, this soft, differentiable filtering preserves information integrity and ensures gradient continuity, establishing a robust ROI-focused foundation for subsequent multi-task balancing.

从统计角度看，Equation (16) 实现了一种聚焦式加权 likelihood estimation。权重 $\tilde{s}_{i,k}$ 根据目标接近程度调节每个观测值对后验推断的贡献（Figure \ref{fig:2}）。该机制在放大关键区域更新信号的同时削弱无关数据波动的干扰，从而自适应地将拟合资源重新分配到 ROI。不同于硬阈值筛选，这种软的、可微的过滤方式保留了信息完整性并保证梯度连续性，为后续 multi-task balancing 建立稳健的 ROI 聚焦基础。

\begin{figure}[htbp]
	\centering
	\includegraphics[width=0.7\linewidth]{FIG2.pdf}
	\caption{Schematic of the sample-level attention mechanism.}
	\caption*{样本级注意力机制示意图。}
	\label{fig:2}
\end{figure}

\subsection{Meta-Learning Driven Task-Level Dynamic Attention} \label{s:sec3.2}
While sample-level attention ensures ROI-focused precision, the macroscopic challenge of conflicting objectives persists. In RPD, competing tasks often induce opposing gradient updates; consequently, static weighting strategies allow tasks with larger gradient magnitudes to dominate, impeding Pareto optimization. To resolve this, a meta-learning-driven dynamic task attention mechanism is introduced. Distinct from heuristic approaches like GradNorm \citep{Chen2018}, this method formulates a bilevel optimization problem. By leveraging feedback from an independent validation set, optimal task weights are learned adaptively to achieve end-to-end multi-task balancing.

尽管样本级注意力保证了 ROI 聚焦精度，但目标冲突这一宏观挑战依然存在。在 RPD 中，竞争任务往往诱导相反的梯度更新；因此，静态加权策略会使梯度幅值较大的任务占据主导，从而阻碍 Pareto optimization。为解决这一问题，本文引入一种 meta-learning 驱动的动态任务注意力机制。不同于 GradNorm 等启发式方法 \citep{Chen2018}，该方法将问题形式化为一个 bilevel optimization problem。通过利用独立验证集反馈，模型能够自适应学习最优任务权重，从而实现端到端 multi-task balancing。

\textbf{Construction of Bilevel Optimization Objective.} To achieve differentiable optimization of task weights, a learnable meta-parameter vector $\boldsymbol{\lambda} = [\lambda_1, \ldots, \lambda_p]^T \in \mathbb{R}^p$ is introduced. To satisfy the constraints that weights must be non-negative and sum to 1 (i.e., $\sum w_k = 1, w_k \ge 0$), the Softmax function is employed to map the meta-parameters to the final task attention weights $\mathbf{w}$:

\textbf{双层优化目标构建。} 为实现任务权重的可微优化，引入可学习 meta-parameter 向量 $\boldsymbol{\lambda} = [\lambda_1, \ldots, \lambda_p]^T \in \mathbb{R}^p$。为满足权重非负且和为 1 的约束（即 $\sum w_k = 1, w_k \ge 0$），本文采用 Softmax function 将 meta-parameters 映射为最终任务注意力权重 $\mathbf{w}$：
\begin{equation}
	{{w}_{k}}(\boldsymbol{\lambda })=\frac{\exp ({{\lambda }_{k}})}{\sum\limits_{j=1}^{p}{\exp }({{\lambda }_{j}})},\quad k=1,\ldots ,p\text{ }.
\end{equation}
On this basis, the optimization process is formalized as a nested bilevel structure:

在此基础上，优化过程被形式化为嵌套的双层结构：

\textit{Inner Loop}: Responsible for updating the model parameters $\boldsymbol{\theta}$ of the DA-DGP (including variational parameters and inducing points). Its objective is to minimize the weighted loss function $\mathcal{L}_{\text{train}}$ on the training set. Here, the sample-weighted loss $\mathcal{L}'_k(\boldsymbol{\theta})$ derived in Equation (15) of Section 3.1 is directly invoked:

\textit{内循环}：负责更新 DA-DGP 的模型参数 $\boldsymbol{\theta}$（包括变分参数和 inducing points）。其目标是在训练集上最小化加权损失函数 $\mathcal{L}_{\text{train}}$。这里直接调用 Section 3.1 中 Equation (15) 推导得到的样本加权损失 $\mathcal{L}'_k(\boldsymbol{\theta})$：
\begin{equation}
	{{\mathcal{L}}_{\text{train}}}(\boldsymbol{\theta },\boldsymbol{\lambda })=\sum\limits_{k=1}^{p}{{{w}_{k}}}(\boldsymbol{\lambda })\cdot {{{\mathcal{L}}'}_{k}}(\boldsymbol{\theta };{{\mathcal{D}}_{\text{train}}}).
\end{equation}

\textit{Outer Loop}: Responsible for updating the task weight meta-parameters $\boldsymbol{\lambda}$. Its objective is to maximize the generalization performance of the model on an independent validation set $\mathcal{D}_{\text{val}}$. The meta-objective function $\mathcal{L}_{\text{meta}}$ is defined as the weighted sum of task losses on the validation set:

\textit{外循环}：负责更新任务权重 meta-parameters $\boldsymbol{\lambda}$。其目标是在独立验证集 $\mathcal{D}_{\text{val}}$ 上最大化模型泛化性能。meta-objective function $\mathcal{L}_{\text{meta}}$ 定义为验证集上任务损失的加权和：
\begin{equation}
	{{\mathcal{L}}_{\text{meta}}}({{\boldsymbol{\theta }}^{*}}(\boldsymbol{\lambda }))=\sum\limits_{k=1}^{p}{{{\pi }_{k}}}\cdot {{{\mathcal{L}}'}_{k}}({{\boldsymbol{\theta }}^{*}}(\boldsymbol{\lambda });{{\mathcal{D}}_{\text{val}}}),
\end{equation}
where $\pi_k$ ($\sum \pi_k = 1$) encodes the static engineering preference on the validation set, typically defaulting to $1/p$ for unbiased fitting. \textcolor{red}{In the reported case studies, the same per-task weighted ELBO structure is evaluated on the full validation split, and all response tasks are included in the primary-task set, so $\pi_k = 1/p$.} A critical distinction exists between $\pi_k$ and the dynamic training weights $\boldsymbol{\lambda}$: while $\boldsymbol{\lambda}$ adaptively modulates the optimization trajectory, $\pi_k$ defines the target equilibrium on the Pareto frontier. With $\boldsymbol{\theta}^*(\boldsymbol{\lambda}) = \arg \min_{\boldsymbol{\theta}} \mathcal{L}_{\text{train}}(\boldsymbol{\theta}, \boldsymbol{\lambda})$ representing the inner-loop solution, the global bilevel optimization objective is formulated as:

其中，$\pi_k$（$\sum \pi_k = 1$）编码验证集上的静态工程偏好，通常默认为 $1/p$ 以实现无偏拟合。\textcolor{red}{在本文两个案例中，同样的按任务加权 ELBO 结构会在完整验证集上被评估，并且全部响应任务均纳入 primary-task 集合，因此 $\pi_k = 1/p$。}$\pi_k$ 与动态训练权重 $\boldsymbol{\lambda}$ 之间存在关键区别：$\boldsymbol{\lambda}$ 自适应调节优化轨迹，而 $\pi_k$ 定义 Pareto frontier 上的目标平衡点。令 $\boldsymbol{\theta}^*(\boldsymbol{\lambda}) = \arg \min_{\boldsymbol{\theta}} \mathcal{L}_{\text{train}}(\boldsymbol{\theta}, \boldsymbol{\lambda})$ 表示内循环解，则全局双层优化目标可写为：
\begin{equation}
	\begin{aligned}
		& \min_{\boldsymbol{\lambda}} \mathcal{L}_{\text{meta}}(\boldsymbol{\theta}^*(\boldsymbol{\lambda})) \\
		& \text{s.t.} \quad \boldsymbol{\theta}^*(\boldsymbol{\lambda}) = \arg \min_{\boldsymbol{\theta}} \mathcal{L}_{\text{train}}(\boldsymbol{\theta}, \boldsymbol{\lambda})
	\end{aligned}.
\end{equation}

\textbf{Solution Strategy Based on One-step Gradient Look-ahead.} Given the computational intractability of deriving the exact inner optimum $\boldsymbol{\theta}^*(\boldsymbol{\lambda})$, a one-step gradient look-ahead approximation is employed. At training step $t$, a virtual parameter update is computed using the current weights $\boldsymbol{\lambda}$:

\textbf{基于一步梯度前瞻的求解策略。} 鉴于精确求解内层最优解 $\boldsymbol{\theta}^*(\boldsymbol{\lambda})$ 在计算上不可行，本文采用一步梯度前瞻近似。在训练步 $t$，使用当前权重 $\boldsymbol{\lambda}$ 计算虚拟参数更新：
\begin{equation}
	\boldsymbol{{\theta }'}(\boldsymbol{\lambda })=\boldsymbol{\theta }-\alpha {{\nabla }_{\boldsymbol{\theta }}}{{\mathcal{L}}_{\text{train}}}(\boldsymbol{\theta },\boldsymbol{\lambda }).
\end{equation}
where $\alpha$ denotes the inner-loop learning rate. \textcolor{red}{The look-ahead parameter $\boldsymbol{\theta}'$ is used only to estimate the meta-gradient and is not committed as the next model parameter value.} At this point, the difficulty of solving Equation (20) transforms into computing the gradient of the meta-objective function with respect to weights $\boldsymbol{\lambda}$. According to the chain rule, this gradient depends on the sensitivity of the updated virtual parameters $\boldsymbol{\theta}'$ to $\boldsymbol{\lambda}$:

其中，$\alpha$ 表示内循环学习率。\textcolor{red}{前瞻参数 $\boldsymbol{\theta}'$ 仅用于估计 meta-gradient，而不会被直接提交为下一步真实模型参数。}此时，求解 Equation (20) 的难点转化为计算 meta-objective function 关于权重 $\boldsymbol{\lambda}$ 的梯度。根据 chain rule，该梯度取决于更新后的虚拟参数 $\boldsymbol{\theta}'$ 对 $\boldsymbol{\lambda}$ 的敏感性：
\begin{equation}
	{{\nabla }_{\boldsymbol{\lambda }}}{{\mathcal{L}}_{\text{meta}}}={{\nabla }_{{\boldsymbol{{\theta }'}}}}{{\mathcal{L}}_{\text{meta}}}\cdot \frac{\partial \boldsymbol{{\theta }'}}{\partial \boldsymbol{\lambda }}.
\end{equation}

Noting that $\boldsymbol{\theta}'$ implicitly depends on $\boldsymbol{\lambda}$. Differentiating both sides of Equation (21) with respect to $\boldsymbol{\lambda}$ (treating $\boldsymbol{\theta}$ as a constant from the previous step), the Jacobian matrix is given by:

注意到 $\boldsymbol{\theta}'$ 隐式依赖于 $\boldsymbol{\lambda}$。对 Equation (21) 两侧关于 $\boldsymbol{\lambda}$ 求导（将 $\boldsymbol{\theta}$ 视为上一时刻的常量），可得到 Jacobian matrix：
\begin{equation}
	\frac{\partial \boldsymbol{{\theta }'}}{\partial \boldsymbol{\lambda }}=-\alpha \nabla _{\boldsymbol{\theta },\boldsymbol{\lambda }}^{2}{{\mathcal{L}}_{\text{train}}}.
\end{equation}

Substituting Equation (23) back into Equation (22), the analytical expression for the meta-gradient is obtained:

将 Equation (23) 代回 Equation (22)，可得到 meta-gradient 的解析表达式：
\begin{equation}
	{{\nabla }_{\boldsymbol{\lambda }}}{{\mathcal{L}}_{\text{meta}}}\approx -\alpha {{\nabla }_{{\boldsymbol{{\theta }'}}}}{{\mathcal{L}}_{\text{meta}}}\cdot \nabla _{\boldsymbol{\theta },\boldsymbol{\lambda }}^{2}{{\mathcal{L}}_{\text{train}}}.
\end{equation}
Directly computing the Hessian matrix $\nabla_{\boldsymbol{\theta}, \boldsymbol{\lambda}}^2 \mathcal{L}_{\text{train}}$ is infeasible in high-dimensional parameter spaces. Therefore, this paper utilizes the finite difference method to efficiently approximate the Hessian-vector product. Given the direction vector $\mathbf{v} = \nabla_{\boldsymbol{\theta}'} \mathcal{L}_{\text{meta}}$, the second-order derivative term can be approximated as:

在高维参数空间中直接计算 Hessian matrix $\nabla_{\boldsymbol{\theta}, \boldsymbol{\lambda}}^2 \mathcal{L}_{\text{train}}$ 不可行。因此，本文利用 finite difference method 高效近似 Hessian-vector product。给定方向向量 $\mathbf{v} = \nabla_{\boldsymbol{\theta}'} \mathcal{L}_{\text{meta}}$，二阶导数项可近似为：
\begin{equation}
	\nabla _{\boldsymbol{\theta },\boldsymbol{\lambda }}^{2}{{\mathcal{L}}_{\text{train}}}\cdot \mathbf{v}\approx \frac{{{\nabla }_{\boldsymbol{\lambda }}}{{\mathcal{L}}_{\text{train}}}(\boldsymbol{\theta }+\epsilon \mathbf{v},\boldsymbol{\lambda })-{{\nabla }_{\boldsymbol{\lambda }}}{{\mathcal{L}}_{\text{train}}}(\boldsymbol{\theta }-\epsilon \mathbf{v},\boldsymbol{\lambda })}{2\epsilon }.
\end{equation}
Finally, the task weights $\boldsymbol{\lambda}$ are updated using this approximate gradient:

最后，使用该近似梯度更新任务权重 $\boldsymbol{\lambda}$：
\begin{equation}
	\boldsymbol{\lambda }\leftarrow \boldsymbol{\lambda }-\beta {{\hat{\nabla }}_{\boldsymbol{\lambda }}}{{\mathcal{L}}_{\text{meta}}},
\end{equation}
where $\beta$ denotes the meta-learning rate, and $\hat{\nabla}_{\boldsymbol{\lambda}} \mathcal{L}_{\text{meta}}$ is the estimated gradient calculated by combining Equations (24) and (25).

其中，$\beta$ 表示 meta-learning rate，$\hat{\nabla}_{\boldsymbol{\lambda}} \mathcal{L}_{\text{meta}}$ 为结合 Equations (24) 和 (25) 计算得到的估计梯度。

\textbf{Algorithm Workflow.} The proposed method orchestrates a coupled dynamic optimization loop via alternating updates. In the inner loop, model parameters $\boldsymbol{\theta}$ are optimized against the weighted training loss to exploit ROI-relevant data features. Simultaneously, the outer loop leverages validation feedback to compute meta-gradients, dynamically rectifying task weights $\boldsymbol{\lambda}$ to prevent overfitting and autonomously navigate the Pareto frontier.

\textbf{算法流程。} 所提方法通过交替更新组织一个耦合的动态优化循环。在内循环中，模型参数 $\boldsymbol{\theta}$ 基于加权训练损失进行优化，以利用 ROI 相关数据特征。与此同时，外循环利用验证反馈计算 meta-gradients，动态修正任务权重 $\boldsymbol{\lambda}$，以防止过拟合并自主导航 Pareto frontier。

Despite the overhead of bilevel optimization, the computational cost remains feasible for RPD (typically $N<1000$), as it is dominated by the inherent complexity of GP inference. \textcolor{red}{For the shared sparse variational DGP backbone, the dominant per-batch cost remains the standard sparse-DGP term of order $B M^2 + M^3$, where $B$ is the batch size and $M$ is the number of inducing points. Relative to ordinary shared DGP training, DA-DGP mainly incurs an additional constant-factor overhead from one virtual gradient step, one validation backward pass, and a finite-difference Hessian-vector approximation; later engineering experiments compare this extra cost empirically with Equal, MGDA, and Indep-DGP baselines.} To ensure numerical stability, the implementation incorporates: (i) gradient clipping (threshold 1.0) to prevent explosion; (ii) decoupled learning rates ($\eta_{\theta} \gg \eta_{\lambda}$) to establish a stable ``fast-parameter, slow-weight'' dynamic; and (iii) Softmax activation to enforce weight non-negativity. The complete procedure is summarized in Algorithm \ref{alg:da_dgp}.

尽管 bilevel optimization 会带来额外开销，但其计算成本对于 RPD 仍然可行（通常 $N<1000$），因为总体开销主要由 GP inference 的固有复杂度主导。\textcolor{red}{对于共享式稀疏变分 DGP 主干而言，每个 batch 的主导计算量仍然是标准 sparse-DGP 的 $B M^2 + M^3$ 项，其中 $B$ 为 batch size，$M$ 为 inducing points 数量。相较于普通共享 DGP 训练，DA-DGP 的额外代价主要来自一次虚拟梯度更新、一次验证集反向传播以及一次 finite-difference Hessian-vector 近似；后续工程实验会把这部分额外开销与 Equal、MGDA 和 Indep-DGP 基线做实证比较。}为确保数值稳定性，实现中包含：（i）gradient clipping（阈值 1.0）以防止梯度爆炸；（ii）解耦学习率（$\eta_{\theta} \gg \eta_{\lambda}$）以建立稳定的“快参数、慢权重”动态；（iii）Softmax activation 以强制权重非负。完整流程总结于 Algorithm \ref{alg:da_dgp}。

\begin{algorithm}[!htbp]
	\caption{DA-DGP modeling workflow.}
	\caption*{DA-DGP 建模流程。}
	\label{alg:da_dgp}
	\begin{spacing}{0.82}
	\footnotesize
%		\hrule
		\vspace{-0.2\baselineskip}
	\begin{algorithmic}[1] % [1] 表示显示行号
		\Require Training set $\mathcal{D}_{\text{train}}$; Validation set $\mathcal{D}_{\text{val}}$; Number of tasks $p$; \textcolor{red}{Sample-attention target values} $\{y_{k}^{*}\}_{k=1}^{p}$; Gaussian kernel bandwidths $\{\sigma _{k}\}_{k=1}^{p}$; Validation preference weights $\{\pi _{k}\}_{k=1}^{p}$; Learning rates $\alpha, \beta$; Gradient clipping thresholds $\tau_{\text{in}}, \tau_{\text{out}}$; Finite difference perturbation step $\epsilon$; Maximum iterations $T$.\\
		\textbf{输入：}训练集 $\mathcal{D}_{\text{train}}$；验证集 $\mathcal{D}_{\text{val}}$；任务数量 $p$；\textcolor{red}{样本注意力目标值} $\{y_{k}^{*}\}_{k=1}^{p}$；Gaussian kernel bandwidths $\{\sigma _{k}\}_{k=1}^{p}$；验证偏好权重 $\{\pi _{k}\}_{k=1}^{p}$；学习率 $\alpha, \beta$；梯度裁剪阈值 $\tau_{\text{in}}, \tau_{\text{out}}$；finite difference 扰动步长 $\epsilon$；最大迭代次数 $T$。
		\Ensure Trained model parameters $\boldsymbol{\theta}^*$; Final task weights $\mathbf{w}^*$.\\
		\textbf{输出：}训练后的模型参数 $\boldsymbol{\theta}^*$；最终任务权重 $\mathbf{w}^*$。
		
		\State Initialize model parameters $\boldsymbol{\theta}$ and task attention meta-parameters $\boldsymbol{\lambda}$
		\State 初始化模型参数 $\boldsymbol{\theta}$ 和任务注意力 meta-parameters $\boldsymbol{\lambda}$
		
		\State \textit{// Pre-computation of Sample-level Weights (Static)}
		\State \textit{// 样本级权重预计算（静态）}
		\For{$k = 1$ to $p$}
		\State Compute raw weights: $s_{i,k} \gets \exp (-\|y_{i,k}-y_{k}^{*}\|^2 / 2\sigma_{k}^{2})$ for all $i=1 \dots N$
		\State 对所有 $i=1 \dots N$ 计算原始权重：$s_{i,k} \gets \exp (-\|y_{i,k}-y_{k}^{*}\|^2 / 2\sigma_{k}^{2})$
		\State Compute normalized weights: $\tilde{s}_{i,k} \gets (s_{i,k} / \sum_{j} s_{j,k}) \cdot N$
		\State 计算归一化权重：$\tilde{s}_{i,k} \gets (s_{i,k} / \sum_{j} s_{j,k}) \cdot N$
		\EndFor
		
		\State \textit{// Main Optimization Loop}
		\State \textit{// 主优化循环}
		\For{$t = 1$ to $T$}
		\State \textit{// Inner Loop: Real Update}
		\State \textit{// 内循环：真实更新}
		\State Compute task weights: $\mathbf{w} \gets \text{softmax}(\boldsymbol{\lambda})$
		\State 计算任务权重：$\mathbf{w} \gets \text{softmax}(\boldsymbol{\lambda})$
		\State Compute weighted training loss: $\mathcal{L}_{\text{train}} \gets \sum_{k=1}^{p} w_{k} \cdot \mathcal{L}'_{k}(\boldsymbol{\theta})$ \Comment{Refer to Eq. (18)}
		\State 计算加权训练损失：$\mathcal{L}_{\text{train}} \gets \sum_{k=1}^{p} w_{k} \cdot \mathcal{L}'_{k}(\boldsymbol{\theta})$ \Comment{见 Eq. (18)}
		\State Compute gradient and clip: $g_{\boldsymbol{\theta}} \gets \text{Clip}(\nabla_{\boldsymbol{\theta}} \mathcal{L}_{\text{train}}, \tau_{\text{in}})$
		\State 计算并裁剪梯度：$g_{\boldsymbol{\theta}} \gets \text{Clip}(\nabla_{\boldsymbol{\theta}} \mathcal{L}_{\text{train}}, \tau_{\text{in}})$
		\State \textcolor{red}{Perform real Adam-style parameter update: $\boldsymbol{\theta} \gets \text{AdamStep}(\boldsymbol{\theta}, g_{\boldsymbol{\theta}}, \alpha)$}
		\State \textcolor{red}{执行真实 Adam 风格参数更新：$\boldsymbol{\theta} \gets \text{AdamStep}(\boldsymbol{\theta}, g_{\boldsymbol{\theta}}, \alpha)$}
		\State \textit{// Virtual Look-ahead for Meta Update}
		\State \textit{// 用于 Meta 更新的虚拟前瞻}
		\State \textcolor{red}{Construct virtual look-ahead copy: $\boldsymbol{\theta}' \gets \text{VirtualAdamStep}(\boldsymbol{\theta}, \mathcal{L}_{\text{train}}, \alpha)$}
		\State \textcolor{red}{构造虚拟前瞻副本：$\boldsymbol{\theta}' \gets \text{VirtualAdamStep}(\boldsymbol{\theta}, \mathcal{L}_{\text{train}}, \alpha)$}
		
		\State \textit{// Outer Loop: Meta Update (Based on Validation Feedback)}
		\State \textit{// 外循环：Meta 更新（基于验证反馈）}
		\State Compute meta-loss on validation set: $\mathcal{L}_{\text{meta}} \gets \sum_{k=1}^{p} \pi_{k} \cdot \mathcal{L}'_{k}(\boldsymbol{\theta}'; \mathcal{D}_{\text{val}})$
		\State 在验证集上计算 meta-loss：$\mathcal{L}_{\text{meta}} \gets \sum_{k=1}^{p} \pi_{k} \cdot \mathcal{L}'_{k}(\boldsymbol{\theta}'; \mathcal{D}_{\text{val}})$
		\State Compute gradient w.r.t virtual parameters: $\mathbf{v} \gets \nabla_{\boldsymbol{\theta}'} \mathcal{L}_{\text{meta}}$
		\State 计算关于虚拟参数的梯度：$\mathbf{v} \gets \nabla_{\boldsymbol{\theta}'} \mathcal{L}_{\text{meta}}$
		\State Approximate Hessian-vector product via Finite Difference: \Comment{Refer to Eq. (25)}
		\State 通过 Finite Difference 近似 Hessian-vector product：\Comment{见 Eq. (25)}
		\Statex \qquad $\mathbf{h} \approx \frac{\nabla_{\boldsymbol{\lambda}} \mathcal{L}_{\text{train}}(\boldsymbol{\theta} + \epsilon \mathbf{v}, \boldsymbol{\lambda}) - \nabla_{\boldsymbol{\lambda}} \mathcal{L}_{\text{train}}(\boldsymbol{\theta} - \epsilon \mathbf{v}, \boldsymbol{\lambda})}{2\epsilon}$
		\State Compute meta-gradient and clip: $g_{\boldsymbol{\lambda}} \gets \text{Clip}(-\alpha \cdot \mathbf{h}, \tau_{\text{out}})$
		\State 计算并裁剪 meta-gradient：$g_{\boldsymbol{\lambda}} \gets \text{Clip}(-\alpha \cdot \mathbf{h}, \tau_{\text{out}})$
		\State Update meta-parameters: $\boldsymbol{\lambda} \gets \boldsymbol{\lambda} - \beta \cdot g_{\boldsymbol{\lambda}}$
		\State 更新 meta-parameters：$\boldsymbol{\lambda} \gets \boldsymbol{\lambda} - \beta \cdot g_{\boldsymbol{\lambda}}$
		
		\State \textcolor{red}{Retain the previously updated real parameters $\boldsymbol{\theta}$; discard $\boldsymbol{\theta}'$ after computing the meta-gradient}
		\State \textcolor{red}{保留前面已更新的真实参数 $\boldsymbol{\theta}$；在完成 meta-gradient 计算后丢弃 $\boldsymbol{\theta}'$}
		\EndFor
		
		\State \Return $\boldsymbol{\theta}^* = \boldsymbol{\theta}$, $\mathbf{w}^* = \text{softmax}(\boldsymbol{\lambda})$
		\State \Return $\boldsymbol{\theta}^* = \boldsymbol{\theta}$，$\mathbf{w}^* = \text{softmax}(\boldsymbol{\lambda})$
	\end{algorithmic}
\end{spacing}
%\hrule
\end{algorithm}

\subsection{Multi-Objective Robust Optimization Model} \label{s:sec3.3}
Following model training, the objective shifts to robust parameter optimization. To simultaneously minimize bias and variance, a multivariate expected quality loss is formulated based on Taguchi's quadratic loss framework \citep{Taguchi1986}. \textcolor{red}{To distinguish this stage from the training-stage attention targets in Section 3.1, let $t_k$ denote the engineering target used by the downstream optimizer.} Leveraging the probabilistic posterior $y_{k}(\mathbf{x})\sim\mathcal{N}(\mu_{k}(\mathbf{x}),\sigma _{k}^{2}(\mathbf{x}))$ provided by DA-DGP, the classic loss function is generalized to the expected quality loss (EQL) for the $k$-th task \citep{Tan2012, Jiang2021}:

模型训练完成后，目标转向鲁棒参数优化。为同时最小化 bias 和 variance，本文基于 Taguchi quadratic loss 框架 \citep{Taguchi1986} 构建多变量 expected quality loss。\textcolor{red}{为与 Section 3.1 中训练阶段的注意力目标相区分，这里记 $t_k$ 为下游优化器使用的工程目标。}利用 DA-DGP 提供的概率后验 $y_{k}(\mathbf{x})\sim\mathcal{N}(\mu_{k}(\mathbf{x}),\sigma _{k}^{2}(\mathbf{x}))$，经典损失函数被推广为第 $k$ 个任务的 expected quality loss（EQL）\citep{Tan2012, Jiang2021}：
\begin{equation}
	E[{L_k}({\bf{x}})] = {C_k}\left[ {{{({\mu _k}({\bf{x}}) - \textcolor{red}{t_k})}^2} + \sigma _k^2({\bf{x}})} \right].
\end{equation}

Equation (27) embodies robust design principles by simultaneously minimizing posterior mean deviation and variance. \textcolor{red}{For clarity, the variance term $\sigma _k^2(\mathbf{x})$ in Equation (27) denotes the posterior uncertainty of the surrogate itself at design point $\mathbf{x}$. If a downstream engineering system contains explicitly simulated exogenous noise factors, the corresponding transmitted variance can be estimated separately during case-specific validation and combined with mean deviation in the final robust assessment.} \textcolor{red}{For multi-objective optimization, the taskwise EQLs are retained as a vector objective:}% [#2/#8]

Equation (27) 通过同时最小化后验均值偏差和方差体现鲁棒设计原则。\textcolor{red}{需要明确的是，Equation (27) 中的方差项 $\sigma _k^2(\mathbf{x})$ 表示设计点 $\mathbf{x}$ 处代理模型自身的后验不确定性。若下游工程系统含有显式仿真的外生噪声因素，则相应的传递方差可在具体案例验证阶段单独估计，并在最终鲁棒评价中与均值偏差结合。}\textcolor{red}{对于 multi-objective optimization，本文保留按任务分解的 EQL 向量目标：}% [#2/#8]
\begin{equation}
\textcolor{red}{\mathbf{J}({\bf{x}})=
\left\{
\begin{array}{c}
J_{1}({\bf{x}})=C_{1}\left[({\mu _1}({\bf{x}})-t_1)^2+\sigma _1^2({\bf{x}})\right]\\
\vdots\\
J_{p}({\bf{x}})=C_{p}\left[({\mu _p}({\bf{x}})-t_p)^2+\sigma _p^2({\bf{x}})\right]
\end{array}
\right.}% [#2/#8]
\end{equation}
\textcolor{red}{The RPD problem is therefore cast as a Pareto optimization problem within the design space $\Omega$:}% [#8]

\textcolor{red}{因此，RPD 问题被表述为设计空间 $\Omega$ 内的 Pareto optimization problem：}% [#8]
\begin{equation}
\textcolor{red}{\mathcal{P}^{*}=\mathop{\arg\min}\limits_{{\bf{x}}\in\Omega}^{\mathrm{Pareto}}\mathbf{J}({\bf{x}}).}% [#8]
\end{equation}
\textcolor{red}{In practice, $\mathcal{P}^{*}$ can be approximated by a suitable multi-objective optimizer, and any subsequent scalar selection rule should be applied only after the Pareto candidate set has been obtained.}% [#8]

\textcolor{red}{在实际求解中，可使用合适的多目标优化器近似 $\mathcal{P}^{*}$；若需要进一步给出单一推荐解，则应在获得 Pareto 候选集之后再施加标量选解规则。}% [#8]

\section{Numerical and Engineering Validation} \label{s:sec4}
\subsection{Numerical Example} \label{s:sec4.1}
To evaluate the proposed method's efficacy in handling high-dimensional inputs and response heterogeneity, a synthetic benchmark with $\mathbf{x} \in [-1, 1]^5$ and three objectives is employed:

为评估所提方法在处理高维输入和响应异质性方面的有效性，本文采用一个输入为 $\mathbf{x} \in [-1, 1]^5$、包含三个目标的合成基准函数：
\begin{equation}
	\mathbf{y}(\mathbf{x})=\left[ \begin{matrix}
		{{y}_{1}}(\mathbf{x})  \\
		{{y}_{2}}(\mathbf{x})  \\
		{{y}_{3}}(\mathbf{x})  \\
	\end{matrix} \right]=\left[ \begin{matrix}
		\sin ({{x}_{1}})+0.5x_{2}^{2}-{{x}_{3}}+{{e}^{-{{x}_{4}}}}+\ln (1+x_{5}^{2})  \\
		{{x}_{1}}{{x}_{5}}+\cos ({{x}_{2}})+\sqrt{|{{x}_{3}}|+1}-\tanh ({{x}_{4}})  \\
		{{e}^{-0.5x_{1}^{2}}}+\ln (1+|{{x}_{2}}|)-{{x}_{3}}{{x}_{4}}+{{(|{{x}_{5}}|+0.1)}^{1/2}}  \\
	\end{matrix} \right].
\end{equation}
These responses represent escalating levels of complexity: $y_1$ serves as a smooth, separable baseline; $y_2$ introduces variable interactions and non-smoothness; and $y_3$ exhibits significant non-convexity and complex coupling. This structural heterogeneity challenges standard fixed-weight optimization, thereby serving as a rigorous testbed for the proposed multi-attention mechanism. The optimization targets are set as $\mathbf{T}=[0, 1.5, 0.8]^T$.

这些响应代表逐渐递增的复杂度水平：$y_1$ 作为平滑且可分离的基线；$y_2$ 引入变量交互和非光滑性；$y_3$ 则表现出显著非凸性和复杂耦合。这种结构异质性对标准固定权重优化构成挑战，因此可作为检验所提 multi-attention 机制的严格 testbed。优化目标设定为 $\mathbf{T}=[0, 1.5, 0.8]^T$。

\textbf{Evaluation Metrics and Baselines.} To assess both \textcolor{red}{ROI predictive fidelity} and \textcolor{red}{probabilistic calibration}, three metrics are reported on the \textcolor{red}{ROI-filtered test subset}: Root Mean Square Error (RMSE), Negative Log Predictive Density (NLPD), and Quality Loss (QL). The ROI is defined by \textcolor{red}{$|y_k-T_k|\leq 0.3$ for $k=1,2,3$}, so the evaluation focuses on \textcolor{red}{ROI performance rather than global test-domain averages}. The comparison includes \textcolor{red}{ten methods}: the proposed \textcolor{red}{DA-DGP}; the optimization baselines \textcolor{red}{DWA, UW, and MGDA \citep{Liu2019,Kendall2018,Sener2018}}; the ablation chain \textcolor{red}{T-Atten, S-Atten (Equal), and Pure DGP}; and the strong Gaussian process surrogate baselines \textcolor{red}{LMC-DGP, Indep-DGP, and Indep-HetGP}. Here, \textcolor{red}{T-Atten keeps only task-level dynamic weighting}, \textcolor{red}{S-Atten (Equal) keeps sample-level attention but replaces meta-learned task weights with uniform coefficients}, and \textcolor{red}{Pure DGP removes both attention modules}.

\textbf{评估指标与基线方法。} 为同时评估\textcolor{red}{ROI 预测精度}与\textcolor{red}{概率校准能力}，本文在\textcolor{red}{经过 ROI 筛选的测试子集}上报告三个指标：Root Mean Square Error（RMSE）、Negative Log Predictive Density（NLPD）和 Quality Loss（QL）。其中，ROI 定义为\textcolor{red}{$|y_k-T_k|\leq 0.3,\ k=1,2,3$}，因此这里衡量的是\textcolor{red}{ROI 性能，而不是全测试域平均表现}。对比对象共\textcolor{red}{10 种方法}：所提\textcolor{red}{DA-DGP}；优化基线\textcolor{red}{DWA、UW 和 MGDA \citep{Liu2019,Kendall2018,Sener2018}}；消融链\textcolor{red}{T-Atten、S-Atten（Equal）和 Pure DGP}；以及强高斯过程代理基线\textcolor{red}{LMC-DGP、Indep-DGP 和 Indep-HetGP}。其中，\textcolor{red}{T-Atten 仅保留任务级动态加权}，\textcolor{red}{S-Atten（Equal）保留样本级注意力但将元学习任务权重替换为均匀系数}，\textcolor{red}{Pure DGP 则同时移除两类注意力模块}。

\textbf{Experimental Setup.} The benchmark uses \textcolor{red}{400 LHS training samples}, \textcolor{red}{400 independent validation samples}, and \textcolor{red}{100000 test samples}. The default surrogate is a \textcolor{red}{2-layer multi-output DGP} with \textcolor{red}{hidden dimension 5} and \textcolor{red}{128 inducing points}. All statistics in this subsection are summarized over \textcolor{red}{20 independent runs}. Figure 3 examines training stability by tracking \textcolor{red}{the dynamic task weights}, \textcolor{red}{the output scales $\sigma_f^2$}, and \textcolor{red}{the ARD length-scales $l$}.

\textbf{实验设置。} 该基准案例采用\textcolor{red}{400 个 LHS 训练样本}、\textcolor{red}{400 个独立验证样本}和\textcolor{red}{100000 个测试样本}。默认代理模型为\textcolor{red}{两层 multi-output DGP}，其\textcolor{red}{隐藏维度为 5}，并使用\textcolor{red}{128 个 inducing points}。本小节全部统计结果均基于\textcolor{red}{20 次独立运行}汇总得到。Figure 3 通过跟踪\textcolor{red}{动态任务权重}、\textcolor{red}{输出尺度 $\sigma_f^2$}和\textcolor{red}{ARD 长度尺度 $l$}来检验训练稳定性。

\begin{figure}[htb]
	\centering
	% 第一张子图 (a)
	\begin{subfigure}[b]{0.3\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG3(a).pdf}
		\caption{Variation of $\sigma_f^2$}
		\caption*{$\sigma_f^2$ 的变化}
		\label{fig:sigma_variation}		
	\end{subfigure}
	\hfill % 弹性填充，让图片分布更均匀
	% 第二张子图 (b)
	\begin{subfigure}[b]{0.3\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG3(b).pdf}
		\caption{Variation of $l$}
		\caption*{$l$ 的变化}
		\label{fig:lengthscale_variation}
	\end{subfigure}
	\hfill
	% 第三张子图 (c)
	\begin{subfigure}[b]{0.3\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG3(c).pdf}

		\caption{Variation of task weights}
		\caption*{任务权重的变化}
		\label{fig:task_weights}
	\end{subfigure}
	
	\caption{Evolution trajectories of metrics during training.}
	\caption*{训练过程中指标的演化轨迹。}
	\label{fig:evolution_metrics}
\end{figure}



Figures 3(a)-(b) depict \textcolor{red}{an initial exploration stage followed by stable convergence} of the output scales ($\sigma_f^2$) and ARD length-scales ($l$), indicating that the variational training process reaches \textcolor{red}{a consistent stationary solution across runs}. Figure 3(c) shows that \textcolor{red}{Task 3 receives the largest dynamic weight in the displayed trajectory}, while \textcolor{red}{Tasks 1 and 2 retain smaller but nonzero shares}. This pattern indicates that \textcolor{red}{the meta-learning layer reallocates training emphasis toward the relatively more demanding objective}, without collapsing the remaining tasks. Quantitative ROI-filtered metrics averaged over \textcolor{red}{20 independent runs} are summarized in Tables 1-3.

Figures 3(a)-(b) 展示了输出尺度（$\sigma_f^2$）和 ARD 长度尺度（$l$）\textcolor{red}{先探索、后稳定收敛}的演化轨迹，说明变分训练过程在不同运行中能够达到\textcolor{red}{一致的稳定驻点解}。Figure 3(c) 表明，在当前图示轨迹中，\textcolor{red}{Task 3 获得最大的动态权重}，而\textcolor{red}{Task 1 与 Task 2 保持较小但非零的权重份额}。这一模式说明\textcolor{red}{元学习层会将训练关注度重新分配给相对更困难的目标}，同时又不会使其余任务被完全忽略。基于\textcolor{red}{20 次独立运行}得到的 ROI 筛选定量指标汇总见 Tables 1-3。


\begin{table}[htbp]
	\centering
	\small
	\caption{\textcolor{red}{Mean $\pm$ std} of \textcolor{red}{ROI-filtered metrics} for Task 1 ($y_1$) \textcolor{red}{over 20 independent runs}.}
	\caption*{\textcolor{red}{20 次独立运行下} Task 1（$y_1$）\textcolor{red}{ROI 筛选指标的均值 $\pm$ 标准差}。}
	\label{tab:results_task1}
	\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lccc}
		\toprule
		\textbf{\textcolor{red}{Method}} & \textbf{\textcolor{red}{RMSE}} & \textbf{\textcolor{red}{NLPD}} & \textbf{\textcolor{red}{QL}} \\
		\midrule
		\textcolor{red}{DA-DGP} & \textcolor{red}{\textbf{0.0384 $\pm$ 0.0124}} & \textcolor{red}{\textbf{-0.8517 $\pm$ 0.0403}} & \textcolor{red}{\textbf{0.0290 $\pm$ 0.0023}} \\
		\textcolor{red}{T-Atten} & \textcolor{red}{0.0451 $\pm$ 0.0123} & \textcolor{red}{-0.8466 $\pm$ 0.0606} & \textcolor{red}{0.0294 $\pm$ 0.0034} \\
		\textcolor{red}{S-Atten (Equal)} & \textcolor{red}{0.0451 $\pm$ 0.0129} & \textcolor{red}{-0.4888 $\pm$ 0.0426} & \textcolor{red}{0.0600 $\pm$ 0.0050} \\
		\textcolor{red}{Pure DGP} & \textcolor{red}{0.0537 $\pm$ 0.0128} & \textcolor{red}{-0.4913 $\pm$ 0.0527} & \textcolor{red}{0.0598 $\pm$ 0.0062} \\
		\textcolor{red}{DWA} & \textcolor{red}{0.0483 $\pm$ 0.0104} & \textcolor{red}{-0.3742 $\pm$ 0.0268} & \textcolor{red}{0.0754 $\pm$ 0.0040} \\
		\textcolor{red}{UW} & \textcolor{red}{0.0460 $\pm$ 0.0136} & \textcolor{red}{-0.5056 $\pm$ 0.0597} & \textcolor{red}{0.0582 $\pm$ 0.0070} \\
		\textcolor{red}{MGDA} & \textcolor{red}{0.0472 $\pm$ 0.0112} & \textcolor{red}{-0.5174 $\pm$ 0.0558} & \textcolor{red}{0.0568 $\pm$ 0.0064} \\
		\textcolor{red}{LMC-DGP} & \textcolor{red}{0.0647 $\pm$ 0.0092} & \textcolor{red}{-0.3337 $\pm$ 0.0917} & \textcolor{red}{0.0829 $\pm$ 0.0164} \\
		\textcolor{red}{Indep-DGP} & \textcolor{red}{0.0784 $\pm$ 0.0196} & \textcolor{red}{-0.3243 $\pm$ 0.2060} & \textcolor{red}{0.0899 $\pm$ 0.0379} \\
		\textcolor{red}{Indep-HetGP} & \textcolor{red}{0.1507 $\pm$ 0.0252} & \textcolor{red}{0.5558 $\pm$ 0.0575} & \textcolor{red}{0.4972 $\pm$ 0.0557} \\
		\bottomrule
	\end{tabular*}
\end{table}

\begin{table}[htbp]
	\centering
	\small
	\caption{\textcolor{red}{Mean $\pm$ std} of \textcolor{red}{ROI-filtered metrics} for Task 2 ($y_2$) \textcolor{red}{over 20 independent runs}.}
	\caption*{\textcolor{red}{20 次独立运行下} Task 2（$y_2$）\textcolor{red}{ROI 筛选指标的均值 $\pm$ 标准差}。}
	\label{tab:results_task2}
	\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lccc}
		\toprule
		\textbf{\textcolor{red}{Method}} & \textbf{\textcolor{red}{RMSE}} & \textbf{\textcolor{red}{NLPD}} & \textbf{\textcolor{red}{QL}} \\
		\midrule
		\textcolor{red}{DA-DGP} & \textcolor{red}{\textbf{0.0382 $\pm$ 0.0097}} & \textcolor{red}{\textbf{-0.8751 $\pm$ 0.0597}} & \textcolor{red}{\textbf{0.0278 $\pm$ 0.0033}} \\
		\textcolor{red}{T-Atten} & \textcolor{red}{0.0570 $\pm$ 0.0142} & \textcolor{red}{-0.8360 $\pm$ 0.0782} & \textcolor{red}{0.0300 $\pm$ 0.0045} \\
		\textcolor{red}{S-Atten (Equal)} & \textcolor{red}{0.0460 $\pm$ 0.0112} & \textcolor{red}{-0.5106 $\pm$ 0.0580} & \textcolor{red}{0.0577 $\pm$ 0.0065} \\
		\textcolor{red}{Pure DGP} & \textcolor{red}{0.0686 $\pm$ 0.0165} & \textcolor{red}{-0.4897 $\pm$ 0.0671} & \textcolor{red}{0.0600 $\pm$ 0.0078} \\
		\textcolor{red}{DWA} & \textcolor{red}{0.0487 $\pm$ 0.0130} & \textcolor{red}{-0.3792 $\pm$ 0.0401} & \textcolor{red}{0.0747 $\pm$ 0.0059} \\
		\textcolor{red}{UW} & \textcolor{red}{0.0464 $\pm$ 0.0107} & \textcolor{red}{-0.5186 $\pm$ 0.0712} & \textcolor{red}{0.0569 $\pm$ 0.0080} \\
		\textcolor{red}{MGDA} & \textcolor{red}{0.0484 $\pm$ 0.0130} & \textcolor{red}{-0.5291 $\pm$ 0.0703} & \textcolor{red}{0.0557 $\pm$ 0.0077} \\
		\textcolor{red}{LMC-DGP} & \textcolor{red}{0.0655 $\pm$ 0.0149} & \textcolor{red}{-0.3532 $\pm$ 0.0595} & \textcolor{red}{0.0789 $\pm$ 0.0093} \\
		\textcolor{red}{Indep-DGP} & \textcolor{red}{0.0820 $\pm$ 0.0252} & \textcolor{red}{-0.3620 $\pm$ 0.2781} & \textcolor{red}{0.0884 $\pm$ 0.0461} \\
		\textcolor{red}{Indep-HetGP} & \textcolor{red}{0.0849 $\pm$ 0.0169} & \textcolor{red}{0.2639 $\pm$ 0.0812} & \textcolor{red}{0.2826 $\pm$ 0.0455} \\
		\bottomrule
	\end{tabular*}
\end{table}

\begin{table}[htbp]
	\centering
	\small
	\caption{\textcolor{red}{Mean $\pm$ std} of \textcolor{red}{ROI-filtered metrics} for Task 3 ($y_3$) \textcolor{red}{over 20 independent runs}.}
	\caption*{\textcolor{red}{20 次独立运行下} Task 3（$y_3$）\textcolor{red}{ROI 筛选指标的均值 $\pm$ 标准差}。}
	\label{tab:results_task3}
	\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lccc}
		\toprule
		\textbf{\textcolor{red}{Method}} & \textbf{\textcolor{red}{RMSE}} & \textbf{\textcolor{red}{NLPD}} & \textbf{\textcolor{red}{QL}} \\
		\midrule
		\textcolor{red}{DA-DGP} & \textcolor{red}{\textbf{0.1360 $\pm$ 0.0260}} & \textcolor{red}{\textbf{-0.5255 $\pm$ 0.1358}} & \textcolor{red}{\textbf{0.0475 $\pm$ 0.0080}} \\
		\textcolor{red}{T-Atten} & \textcolor{red}{0.1703 $\pm$ 0.0324} & \textcolor{red}{-0.3366 $\pm$ 0.2050} & \textcolor{red}{0.0588 $\pm$ 0.0124} \\
		\textcolor{red}{S-Atten (Equal)} & \textcolor{red}{0.1570 $\pm$ 0.0340} & \textcolor{red}{-0.2874 $\pm$ 0.1099} & \textcolor{red}{0.0830 $\pm$ 0.0129} \\
		\textcolor{red}{Pure DGP} & \textcolor{red}{0.2070 $\pm$ 0.0378} & \textcolor{red}{-0.1281 $\pm$ 0.1495} & \textcolor{red}{0.1019 $\pm$ 0.0185} \\
		\textcolor{red}{DWA} & \textcolor{red}{0.1673 $\pm$ 0.0345} & \textcolor{red}{-0.1859 $\pm$ 0.0871} & \textcolor{red}{0.1034 $\pm$ 0.0133} \\
		\textcolor{red}{UW} & \textcolor{red}{0.1606 $\pm$ 0.0354} & \textcolor{red}{-0.2816 $\pm$ 0.1217} & \textcolor{red}{0.0834 $\pm$ 0.0146} \\
		\textcolor{red}{MGDA} & \textcolor{red}{0.1708 $\pm$ 0.0355} & \textcolor{red}{-0.2555 $\pm$ 0.1291} & \textcolor{red}{0.0859 $\pm$ 0.0151} \\
		\textcolor{red}{LMC-DGP} & \textcolor{red}{0.1849 $\pm$ 0.0380} & \textcolor{red}{-0.1248 $\pm$ 0.0989} & \textcolor{red}{0.1144 $\pm$ 0.0174} \\
		\textcolor{red}{Indep-DGP} & \textcolor{red}{0.1882 $\pm$ 0.0475} & \textcolor{red}{-0.0918 $\pm$ 0.2469} & \textcolor{red}{0.1374 $\pm$ 0.0643} \\
		\textcolor{red}{Indep-HetGP} & \textcolor{red}{0.2824 $\pm$ 0.0365} & \textcolor{red}{0.4135 $\pm$ 0.0453} & \textcolor{red}{0.3500 $\pm$ 0.0360} \\
		\bottomrule
	\end{tabular*}
\end{table}



Based on Tables 1-3, \textcolor{red}{DA-DGP records the lowest mean RMSE, NLPD, and QL for all three tasks in this benchmark}. The ablation chain is particularly informative: \textcolor{red}{T-Atten remains close to DA-DGP on Task 1 and Task 2}, but \textcolor{red}{the gap widens on the more nonlinear Task 3} (RMSE \textcolor{red}{0.1703} vs. \textcolor{red}{0.1360}; QL \textcolor{red}{0.0588} vs. \textcolor{red}{0.0475}), indicating that \textcolor{red}{sample-level attention contributes most clearly when task complexity and response heterogeneity increase}. In contrast, \textcolor{red}{S-Atten (Equal) and Pure DGP show clear deterioration in NLPD and QL}, suggesting that \textcolor{red}{task balancing and ROI-oriented sample reweighting are complementary rather than interchangeable}. Among the strong Gaussian process surrogate baselines, \textcolor{red}{LMC-DGP is the most competitive}, yet it still remains behind DA-DGP on every task, while \textcolor{red}{Indep-DGP and Indep-HetGP show larger losses in accuracy and calibration}, especially on Task 3.

基于 Tables 1-3，\textcolor{red}{DA-DGP 在该基准中三个任务的平均 RMSE、NLPD 和 QL 均最低}。其中，消融链给出了最直接的证据：\textcolor{red}{T-Atten 在 Task 1 和 Task 2 上已接近 DA-DGP}，但在\textcolor{red}{非线性更强的 Task 3 上差距明显扩大}（RMSE 为\textcolor{red}{0.1703} 对比 \textcolor{red}{0.1360}；QL 为\textcolor{red}{0.0588} 对比 \textcolor{red}{0.0475}），说明\textcolor{red}{当任务复杂度和响应异质性升高时，样本级注意力的贡献最为清晰}。相较之下，\textcolor{red}{S-Atten（Equal）和 Pure DGP 在 NLPD 与 QL 上明显退化}，表明\textcolor{red}{任务平衡与面向 ROI 的样本重加权具有互补作用，而非彼此替代}。在强高斯过程代理基线中，\textcolor{red}{LMC-DGP 的竞争力最强}，但其在所有任务上仍落后于 DA-DGP，而\textcolor{red}{Indep-DGP 与 Indep-HetGP 在精度和校准上损失更大}，尤其是在 Task 3 上更为明显。

From the uncertainty perspective, \textcolor{red}{DA-DGP preserves the lowest NLPD across the three tasks}, which indicates that \textcolor{red}{its predictive variances are better aligned with the residual structure inside the ROI}. From the design-goal perspective, \textcolor{red}{its QL values remain the smallest for all tasks}, suggesting that \textcolor{red}{the proposed dual-attention mechanism improves both ROI bias control and uncertainty management}. Therefore, the numerical example supports \textcolor{red}{the combination of shared deep representation, adaptive task weighting, and target-oriented sample attention} as the most reliable configuration among those evaluated for this heterogeneous benchmark.

从不确定性角度看，\textcolor{red}{DA-DGP 在三个任务上均保持最低 NLPD}，这表明\textcolor{red}{其预测方差与 ROI 内残差结构的匹配程度更高}。从设计目标实现角度看，\textcolor{red}{其 QL 在所有任务上同样最小}，说明\textcolor{red}{所提双注意力机制同时改善了 ROI 偏差控制与不确定性管理}。因此，该数值案例支持\textcolor{red}{“共享深层表示 + 自适应任务加权 + 面向目标的样本注意力”}作为所评估配置中处理异质多响应基准问题最可靠的组合。

\textbf{\textcolor{red}{Sensitivity Analyses.}} \textcolor{red}{Additional sensitivity experiments clarify why the default numerical setting is used. For the Gaussian bandwidth, performance is stable for moderate values: $\sigma=0.5$ gives the lowest average QL (0.034774), while $\sigma=0.3$ gives the lowest average RMSE (0.069403) and remains close in QL; in contrast, the very narrow bandwidth $\sigma=0.1$ increases both RMSE and QL, indicating over-localized sample weighting. For architecture complexity, the default 2L-D5 surrogate gives the lowest average QL (0.034732) among the tested variants, whereas the shallower 2L-D2 underfits and deeper 3L-D5--5L-D5 variants deteriorate rapidly. For sample size, increasing the training budget from $n=20$ to $n=400$ monotonically improves the average QL from 1.439658 to 0.034749, with a large remaining gain from $n=200$ to $n=400$.} \textcolor{red}{Detailed tables, taskwise error-bar plots, and discussion are provided in Appendix E of the Supplementary Materials.}

\textbf{\textcolor{red}{敏感性分析。}} \textcolor{red}{额外敏感性实验进一步说明了正文默认数值设置的合理性。首先，Gaussian 带宽在中等取值下表现稳定：$\sigma=0.5$ 取得最低平均 QL（0.034774），$\sigma=0.3$ 取得最低平均 RMSE（0.069403）且 QL 与 $\sigma=0.5$ 接近；相反，过窄的 $\sigma=0.1$ 会同时提高 RMSE 和 QL，说明样本权重过度收缩到 ROI 中极窄邻域会损害整体学习信号。其次，在结构复杂度方面，默认 2L-D5 代理模型在所测试变体中取得最低平均 QL（0.034732），较浅的 2L-D2 存在欠拟合，而更深的 3L-D5--5L-D5 则快速退化。最后，在样本量方面，训练样本从 $n=20$ 增加到 $n=400$ 时，平均 QL 从 1.439658 单调降至 0.034749，且 $n=200$ 到 $n=400$ 仍有明显收益。} \textcolor{red}{更详细的统计表、分任务误差棒图及分析见 Supplementary Materials 的 Appendix E。}

In summary, \textcolor{red}{the experimental results show that DA-DGP handles high-dimensional response heterogeneity effectively in this benchmark}. By synergizing \textcolor{red}{ROI-oriented sample attention} with \textcolor{red}{global task weighting}, the proposed mechanism improves the \textcolor{red}{bias-variance trade-off} and \textcolor{red}{achieves lower RMSE, NLPD, and QL than the considered baselines}. These findings provide \textcolor{red}{an empirical algorithmic basis} for the subsequent case study: \textcolor{red}{RPD of a 5G/6G OFDM communication system}.

综上，\textcolor{red}{实验结果表明 DA-DGP 能够有效处理该基准中的高维响应异质性}。通过协同\textcolor{red}{面向 ROI 的样本注意力}和\textcolor{red}{全局任务加权}，所提机制改善了\textcolor{red}{bias-variance trade-off}，并在\textcolor{red}{RMSE、NLPD 和 QL 上低于所比较的基线方法}。这些发现为后续案例研究\textcolor{red}{5G/6G OFDM 通信系统的 RPD}提供了\textcolor{red}{经验层面的算法依据}。


\subsection{Robust Parameter Design for OFDM Systems}\label{s:sec5.1}
To evaluate the proposed framework on complex engineering systems, this section presents a case study on the OFDM system, a core 5G/6G physical layer technology. System performance is constrained by strongly conflicting objectives: increasing transmission power improves SNR—enhancing Throughput and reducing BER—but inevitably degrades PAPR and energy consumption. Furthermore, the nonlinear coupling between multipath fading channels and Carrier Frequency Offset (CFO) induces highly non-stationary and heterogeneous response surfaces, \textcolor{red}{and the link is simultaneously perturbed by random bit streams, Rayleigh channel realizations and additive white Gaussian noise (AWGN). The modeling and evaluation workflow must therefore separate the surrogate's epistemic uncertainty from the transmitted variance estimated across repeated noise realizations.}% [#2]

为在复杂工程系统中评估所提框架，本节以 OFDM 系统这一 5G/6G 核心物理层技术为案例开展研究。系统性能受到强冲突目标的约束：提高发射功率会改善 SNR，从而提升 Throughput 并降低 BER，但也不可避免地恶化 PAPR 并增加能耗。多径衰落信道与 Carrier Frequency Offset（CFO）之间的非线性耦合会诱导高度非平稳且异质的响应曲面，\textcolor{red}{并且链路同时受到随机比特流、Rayleigh 信道实现与加性高斯白噪声（AWGN）的扰动。因此，建模与评价流程需要区分代理模型的认知不确定性，以及通过重复噪声 realization 估计得到的传递方差。}% [#2]

\begin{figure}[htbp]
	\centering
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG4(a).pdf} 
		\caption{OFDM signal structure}
		\caption*{OFDM 信号结构}
		\label{fig:ofdm_signals}
	\end{subfigure}
%	\hfill 
	\begin{subfigure}[b]{0.35\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG4(b).pdf}
		\caption{Pinching antenna system}
		\caption*{Pinching 天线系统}
		\label{fig:pinching_system}
	\end{subfigure}

	\caption{Schematic diagrams of the proposed system.}
	\caption*{所提系统的示意图。}
	\label{fig:combined_system}
\end{figure}

A high-fidelity OFDM simulation platform (Figure 4) is constructed operating at 3.5 GHz, employing 256-point FFT, 64-point CP, and 16-QAM. To characterize the impact of design factors $\mathbf{x}={{[{{x}_{1}},\ldots ,{{x}_{4}}]}^{T}}$ on responses $\mathbf{y}$, the simulation integrates three core physical mechanisms. First, the Energy Transmission Mechanism (${{x}_{1}},{{x}_{2}}$) determines SNR via transmit power ${{x}_{1}}$ and path loss distance ${{x}_{2}}$:

本文构建了运行于 3.5 GHz 的高保真 OFDM 仿真平台（Figure 4），采用 256 点 FFT、64 点 CP 和 16-QAM。为刻画设计因子 $\mathbf{x}={{[{{x}_{1}},\ldots ,{{x}_{4}}]}^{T}}$ 对响应 $\mathbf{y}$ 的影响，仿真整合了三类核心物理机制。首先，Energy Transmission Mechanism（${{x}_{1}},{{x}_{2}}$）通过发射功率 ${{x}_{1}}$ 和路径损耗距离 ${{x}_{2}}$ 决定 SNR：
\begin{equation}
	SN{R_{dB}} = {x_1} - \left[ 20\log_{10}\!\left( \frac{4\pi {f_c}}{c} \right) + 10\,n_{\mathrm{PL}}\log_{10}\!\left( \frac{{{x_2}}}{{{d_0}}} \right) \right] - N_{\mathrm{floor}},
\end{equation}
necessitating dynamic adjustment of ${{x}_{1}}$ to counteract ${{x}_{2}}$-induced nonlinear attenuation. Second, the Multipath Time Dispersion Mechanism (${{x}_{3}}$) models a Rayleigh fading channel with power delay profile (PDP) decay controlled by RMS delay spread ${{x}_{3}}$ ($PDP(\tau )\propto {{e}^{-\tau /{{x}_{3}}}}$); larger ${{x}_{3}}$ exacerbates frequency-selective fading and Inter-Symbol Interference (ISI), limiting BER. Third, the Spectral Offset Mechanism (${{x}_{4}}$) simulates Carrier Frequency Offset (CFO), introducing phase rotation ${{e}^{j2\pi \varepsilon k}}$ that destroys subcarrier orthogonality and generates Inter-Carrier Interference (ICI), degrading SINR. \textcolor{red}{These mechanisms provide three waveform-level link-quality responses: Throughput (${{y}_{1}}$), BER (${{y}_{2}}$), and PAPR (${{y}_{3}}$). For the four-objective Pareto assessment used later, we additionally define a link-budget energy-efficiency indicator ${{y}_{4}}=1/{{E}_{b}}$, which is computed by a separate post-processing model from transmit power and throughput rather than directly by the waveform simulator.}% [#2/#8] Robustness is tested against challenging targets \textcolor{red}{$\mathbf{T}={{[7.0\text{ Mbps}, 0.2, 6.5\text{ dB}]}^{T}}$}% [#2] set at the feasible region boundaries. \textcolor{red}{For each candidate $\mathbf{x}$, the simulator re-draws the bit stream, the Rayleigh multipath taps and the AWGN over $R=16$ independent Monte Carlo simulations, producing the empirical mean ${{\hat{m}}_{i}}(\mathbf{x})$ and the transmitted variance ${{\hat{v}}_{i}}(\mathbf{x})$ that propagate the exogenous noise to each response.}% [#2]

因此需要动态调整 ${{x}_{1}}$ 以抵消 ${{x}_{2}}$ 引起的非线性衰减。其次，Multipath Time Dispersion Mechanism（${{x}_{3}}$）使用由 RMS delay spread ${{x}_{3}}$ 控制 power delay profile（PDP）衰减的 Rayleigh fading channel（$PDP(\tau )\propto {{e}^{-\tau /{{x}_{3}}}}$）；较大的 ${{x}_{3}}$ 会加剧 frequency-selective fading 和 Inter-Symbol Interference（ISI），从而限制 BER。第三，Spectral Offset Mechanism（${{x}_{4}}$）模拟 Carrier Frequency Offset（CFO），引入相位旋转 ${{e}^{j2\pi \varepsilon k}}$，破坏子载波正交性并产生 Inter-Carrier Interference（ICI），从而降低 SINR。\textcolor{red}{上述机制给出三个波形层面的链路质量响应：Throughput（${{y}_{1}}$）、BER（${{y}_{2}}$）和 PAPR（${{y}_{3}}$）。为后续四目标 Pareto 评价，本文进一步定义链路预算能量效率指标 ${{y}_{4}}=1/{{E}_{b}}$；该指标由独立后处理模型根据发射功率与吞吐量派生得到，并不由当前波形仿真链路直接输出。}% [#2/#8] 鲁棒性测试采用设置在可行域边界处的挑战性目标 \textcolor{red}{$\mathbf{T}={{[7.0\text{ Mbps}, 0.2, 6.5\text{ dB}]}^{T}}$}% [#2]。\textcolor{red}{对每个候选解 $\mathbf{x}$，仿真器在 $R=16$ 次独立蒙特卡洛模拟下重新生成比特流、Rayleigh 多径抽头与 AWGN，从而给出经验均值 ${{\hat{m}}_{i}}(\mathbf{x})$ 和将外部噪声传递到各响应的传递方差 ${{\hat{v}}_{i}}(\mathbf{x})$。}% [#2]

In this experiment, Latin hypercube sampling (LHS) is employed to generate 400 initial samples within the design space $\Omega$ as the training set. \textcolor{red}{The surrogate is a two-layer multi-output DGP with a five-dimensional latent representation and $128$ inducing points per layer, providing sufficient capacity for the four-dimensional input while remaining well-posed on a finite engineering sample budget.}% [#3/#12] The proposed DA-DGP model is then utilized to fit the complex nonlinear mapping $\mathbf{x} \to \mathbf{y}$. Based on the posterior mean $\mu_{i}(\mathbf{x})$ and variance $\sigma_{i}^{2}(\mathbf{x})$ predicted by the model, the expected quality loss (EQL) function for the $i$-th task is constructed as:

在本实验中，采用 Latin hypercube sampling（LHS）在设计空间 $\Omega$ 内生成 400 个初始样本作为训练集。\textcolor{red}{代理模型采用两层 multi-output DGP，潜在表示维度为五，每层使用 $128$ 个 inducing point；该结构在四维输入下保持充足表达能力，同时在有限的工程样本规模下保持良态。}% [#3/#12] 随后使用所提出的 DA-DGP 模型拟合复杂非线性映射 $\mathbf{x} \to \mathbf{y}$。基于模型预测得到的后验均值 $\mu_{i}(\mathbf{x})$ 和方差 $\sigma_{i}^{2}(\mathbf{x})$，第 $i$ 个任务的 expected quality loss（EQL）函数构建为：
\begin{equation}
	{L_i}({\bf{x}}) = {({\mu _i}({\bf{x}}) - y_i^*)^2} + \sigma _i^2({\bf{x}}),\quad i = 1,2,3.
\end{equation}

\textcolor{red}{The posterior variance $\sigma_{i}^{2}(\mathbf{x})$ in Eq.~(32) characterizes the epistemic uncertainty of the DGP surrogate and drives ROI-aware exploration of the multi-objective landscape. Because the OFDM link is additionally perturbed by exogenous noise realizations, the engineering-side performance of any candidate is measured by the robust loss}% [#2]
\begin{equation}\label{eq:robust_loss}
\textcolor{red}{\begin{aligned}
\widetilde{L}_{i}(\mathbf{x}) &= \bigl({{\hat{m}}_{i}}(\mathbf{x}) - y_{i}^{*}\bigr)^{2} + {{\hat{v}}_{i}}(\mathbf{x}),\\
{{\hat{m}}_{i}}(\mathbf{x}) &= \tfrac{1}{R}\sum_{r=1}^{R} f_{i}(\mathbf{x};\xi_{r}),\\
{{\hat{v}}_{i}}(\mathbf{x}) &= \tfrac{1}{R}\sum_{r=1}^{R}\bigl(f_{i}(\mathbf{x};\xi_{r})-{{\hat{m}}_{i}}(\mathbf{x})\bigr)^{2}.
\end{aligned}}% [#2]
\end{equation}
\textcolor{red}{where $\xi_{r}$ collects the random bit stream, the multipath taps and the AWGN drawn in the $r$-th Monte Carlo simulation. Equation~(32) is used inside the NSGA-II search reported in Tables~4--5, while Eq.~(\ref{eq:robust_loss}) defines the robust ranking visualised in Table~7.}% [#2]

\textcolor{red}{Eq.~(32) 中的 $\sigma_{i}^{2}(\mathbf{x})$ 刻画了 DGP 代理的认知不确定性，并在多目标搜索中驱动 ROI 聚焦。由于 OFDM 链路还受到外部噪声实现的扰动，候选解的工程侧性能则由鲁棒损失}% [#2]
\textcolor{red}{衡量，其中 $\xi_{r}$ 统一表示第 $r$ 次蒙特卡洛模拟中重新采样的随机比特流、多径抽头与 AWGN。Eq.~(32) 用于 Tables~4--5 中基于 NSGA-II 的搜索，而 Eq.~(\ref{eq:robust_loss}) 给出 Table~7 中所采用的鲁棒排序。}% [#2]

Subsequently, \textcolor{red}{the non-dominated sorting multi-objective genetic algorithm NSGA-II~\citep{Deb2002} is adopted to search the Pareto front of the quality-loss vector}:% [#8]

随后，\textcolor{red}{采用非支配排序多目标遗传算法 NSGA-II~\citep{Deb2002} 在质量损失向量上搜索 Pareto 前沿}：% [#8]
\begin{equation}
	\textcolor{red}{\min_{\mathbf{x}\in\Omega}\ \mathbf{L}(\mathbf{x}) = \bigl(L_{1}(\mathbf{x}),\, L_{2}(\mathbf{x}),\, L_{3}(\mathbf{x})\bigr)^{\!\top},}% [#8]
	\label{eq:opt_objective}
\end{equation}
\textcolor{red}{over the design space $\Omega=[5,30]\times[50,500]\times[50,500]\times[0,10]$. Each NSGA-II run returns a Pareto set $\mathcal{P}^{(k)}\subset\Omega$ of non-dominated candidates. To select a recommended solution representing the preferred link-performance compromise from each Pareto set, this paper uses a prespecified engineering selection rule: $\mathbf{x}^{\star,(k)}$ is taken as the Pareto candidate that minimizes the following achievement scalarizing function
\begin{equation*}
\mathbf{x}^{\star,(k)}=\arg\min_{\mathbf{x}\in\mathcal{P}^{(k)}}\Big\{\tfrac{y_{1}^{*}-\hat{m}_{1}(\mathbf{x})}{\Delta\hat{m}_{1}} + \tfrac{\hat{m}_{2}(\mathbf{x})-\hat{m}_{2}^{\min}}{\Delta\hat{m}_{2}}\Big\},
\end{equation*}
where $\Delta\hat{m}_{i}$ is the pooled global range of the $i$-th robust mean across all methods and $\hat{m}_{2}^{\min}$ is the pooled global minimum of the robust BER. The first term penalizes the throughput shortfall to the target $y_{1}^{*}=7.0$\,Mbps and the second penalizes BER excursions above the pooled minimum BER, jointly capturing the dominant link-capacity / modulation-quality trade-off; the PAPR is left unconstrained at the recommendation stage and instead serves as an audited downstream variable. This prespecified rule favors Pareto sets that approach the aggressive throughput target while keeping the symbol error rate low, and penalizes narrow Pareto clusters that miss either component.}% [#8]

\textcolor{red}{设计空间为 $\Omega=[5,30]\times[50,500]\times[50,500]\times[0,10]$。每次 NSGA-II 运行输出一个非支配候选集 $\mathcal{P}^{(k)}\subset\Omega$。为从每个 Pareto 集中选取链路表现最好的最优解，本文预设如下工程选解规则：第 $k$ 次运行的最优解 $\mathbf{x}^{\star,(k)}$ 取自使下列 achievement 标量化函数最小的 Pareto 候选
\begin{equation*}
\mathbf{x}^{\star,(k)}=\arg\min_{\mathbf{x}\in\mathcal{P}^{(k)}}\Big\{\tfrac{y_{1}^{*}-\hat{m}_{1}(\mathbf{x})}{\Delta\hat{m}_{1}} + \tfrac{\hat{m}_{2}(\mathbf{x})-\hat{m}_{2}^{\min}}{\Delta\hat{m}_{2}}\Big\},
\end{equation*}
其中 $\Delta\hat{m}_{i}$ 是第 $i$ 个鲁棒均值在全部方法上汇总的全局极差，$\hat{m}_{2}^{\min}$ 是汇总全局最低 BER。第一项惩罚到吞吐目标 $y_{1}^{*}=7.0$\,Mbps 的差距，第二项惩罚相对全局最优 BER 的偏移，二者共同刻画"链路容量--调制质量"主导权衡；PAPR 在推荐阶段不参与硬约束，而作为后续审查的下游变量。该预设规则偏向能够逼近激进吞吐目标且同时保持低误码率的 Pareto 集，并惩罚仅在某一窄区聚集而错失另一分量的 Pareto 簇。}% [#8]

\textcolor{red}{For each method the surrogate is trained once and NSGA-II is then repeated as $K=20$ independent Monte Carlo trials, after which every Pareto candidate is evaluated on $R=16$ independent Monte Carlo simulations of the OFDM link.}% [#11/#12] The results are presented in Tables 4-5.

\textcolor{red}{每种方法的代理模型训练一次，NSGA-II 以 $K=20$ 次独立蒙特卡洛试验重复，所有 Pareto 候选解再在 OFDM 链路的 $R=16$ 次独立蒙特卡洛模拟下评估。}% [#11/#12] 结果见 Tables 4-5。

\begin{table}[!htbp]
	\centering
	\caption{Summary of optimization results and validation comparisons.}
	\caption*{优化结果与验证比较汇总。}
	\label{tab:sim_results}
	\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lccc}
		\toprule
		\textbf{Method} & \textbf{Throughput (Mbps)} & \textbf{BER} & \textbf{PAPR (dB)} \\
		\midrule
		\textbf{DA-DGP}                                          & \textcolor{red}{$\mathbf{6.0526 \pm 0.0815}$}% [#11]
		                                                        & \textcolor{red}{$0.2167 \pm 0.0008$}% [#11]
		                                                        & \textcolor{red}{$8.3581 \pm 0.1348$}\\% [#11]
		Equal                                                   & \textcolor{red}{$6.0140 \pm 0.0956$}% [#11]
		                                                        & \textcolor{red}{$0.2167 \pm 0.0009$}% [#11]
		                                                        & \textcolor{red}{$8.2953 \pm 0.1186$}\\% [#11]
		\textcolor{red}{Pure DGP}% [#7]
		                                                        & \textcolor{red}{$6.0240 \pm 0.1004$}% [#7/#11]
		                                                        & \textcolor{red}{$0.2173 \pm 0.0011$}% [#7/#11]
		                                                        & \textcolor{red}{$8.3115 \pm 0.1649$}\\% [#7/#11]
		DWA                                                     & \textcolor{red}{$5.9926 \pm 0.1348$}% [#11]
		                                                        & \textcolor{red}{$0.2168 \pm 0.0018$}% [#11]
		                                                        & \textcolor{red}{$8.3115 \pm 0.1690$}\\% [#11]
		UW                                                      & \textcolor{red}{$5.9972 \pm 0.1215$}% [#11]
		                                                        & \textcolor{red}{$0.2173 \pm 0.0016$}% [#11]
		                                                        & \textcolor{red}{$8.2979 \pm 0.1662$}\\% [#11]
		MGDA                                                    & \textcolor{red}{$6.0264 \pm 0.0879$}% [#11]
		                                                        & \textcolor{red}{$0.2169 \pm 0.0008$}% [#11]
		                                                        & \textcolor{red}{$8.2768 \pm 0.1077$}\\% [#11]
		\textcolor{red}{Indep-DGP}% [#4/#6]
		                                                        & \textcolor{red}{$6.0082 \pm 0.0872$}% [#4/#11]
		                                                        & \textcolor{red}{$\mathbf{0.2160 \pm 0.0004}$}% [#4/#11]
		                                                        & \textcolor{red}{$8.0860 \pm 0.1494$}\\% [#4/#11]
		\textcolor{red}{Indep-HetGP}% [#4/#6]
		                                                        & \textcolor{red}{$5.8786 \pm 0.0440$}% [#4/#11]
		                                                        & \textcolor{red}{$0.2161 \pm 0.0005$}% [#4/#11]
		                                                        & \textcolor{red}{$\mathbf{7.8455 \pm 0.1607}$}\\% [#4/#11]
		\textcolor{red}{LMC-DGP}% [#6]
		                                                        & \textcolor{red}{$6.0099 \pm 0.0821$}% [#6/#11]
		                                                        & \textcolor{red}{$0.2160 \pm 0.0007$}% [#6/#11]
		                                                        & \textcolor{red}{$8.1830 \pm 0.1195$}\\% [#6/#11]
		\textcolor{red}{T-Atten}% [#7]
		                                                        & \textcolor{red}{$6.0248 \pm 0.0786$}% [#7/#11]
		                                                        & \textcolor{red}{$0.2169 \pm 0.0005$}% [#7/#11]
		                                                        & \textcolor{red}{$8.3067 \pm 0.1497$}\\% [#7/#11]
		\midrule
		\textcolor{red}{BO-qParEGO}% [#9]
		                                                        & \textcolor{red}{$5.9316 \pm 0.1447$}% [#9/#11]
		                                                        & \textcolor{red}{$0.2186 \pm 0.0017$}% [#9/#11]
		                                                        & \textcolor{red}{$8.0286 \pm 0.4379$}\\% [#9/#11]
		\textcolor{red}{BO-qEHVI}% [#9]
		                                                        & \textcolor{red}{$6.1019 \pm 0.0276$}% [#9/#11]
		                                                        & \textcolor{red}{$0.2185 \pm 0.0008$}% [#9/#11]
		                                                        & \textcolor{red}{$8.5804 \pm 0.1779$}\\% [#9/#11]
		\textcolor{red}{BO-qNEHVI}% [#9]
		                                                        & \textcolor{red}{$6.1137 \pm 0.0270$}% [#9/#11]
		                                                        & \textcolor{red}{$0.2183 \pm 0.0006$}% [#9/#11]
		                                                        & \textcolor{red}{$8.5730 \pm 0.1944$}\\% [#9/#11]
		\bottomrule
	\end{tabular*}
\end{table}

\begin{table}[!htbp]
	\centering
	\caption{Statistical summary of QL for different methods.}
	\caption*{不同方法 QL 的统计汇总。}
	\label{tab:quality_loss_results}
	\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lccc}
		\toprule
		\textbf{Method} & \textbf{QL$_{\text{Throughput}}$} & \textbf{QL$_{\text{BER}}\,(\times 10^{-3})$} & \textbf{QL$_{\text{PAPR}}$} \\
		\midrule
		\textbf{DA-DGP}                                          & \textcolor{red}{$\mathbf{0.9086 \pm 0.1677}$}% [#11]
		                                                        & \textcolor{red}{$0.4994 \pm 0.0127$}% [#11]
		                                                        & \textcolor{red}{$3.4740 \pm 0.4955$}\\% [#11]
		Equal                                                   & \textcolor{red}{$0.9856 \pm 0.2003$}% [#11]
		                                                        & \textcolor{red}{$0.5042 \pm 0.0245$}% [#11]
		                                                        & \textcolor{red}{$3.2404 \pm 0.4214$}\\% [#11]
		\textcolor{red}{Pure DGP}% [#7]
		                                                        & \textcolor{red}{$0.9666 \pm 0.2077$}% [#7/#11]
		                                                        & \textcolor{red}{$0.5119 \pm 0.0260$}% [#7/#11]
		                                                        & \textcolor{red}{$3.3114 \pm 0.5851$}\\% [#7/#11]
		DWA                                                     & \textcolor{red}{$1.0371 \pm 0.2957$}% [#11]
		                                                        & \textcolor{red}{$0.5200 \pm 0.0450$}% [#11]
		                                                        & \textcolor{red}{$3.3126 \pm 0.5936$}\\% [#11]
		UW                                                      & \textcolor{red}{$1.0242 \pm 0.2743$}% [#11]
		                                                        & \textcolor{red}{$0.5228 \pm 0.0454$}% [#11]
		                                                        & \textcolor{red}{$3.2625 \pm 0.5829$}\\% [#11]
		MGDA                                                    & \textcolor{red}{$0.9599 \pm 0.1799$}% [#11]
		                                                        & \textcolor{red}{$0.5005 \pm 0.0186$}% [#11]
		                                                        & \textcolor{red}{$3.1719 \pm 0.3782$}\\% [#11]
		\textcolor{red}{Indep-DGP}% [#4/#6]
		                                                        & \textcolor{red}{$0.9958 \pm 0.1841$}% [#4/#11]
		                                                        & \textcolor{red}{$\mathbf{0.4906 \pm 0.0091}$}% [#4/#11]
		                                                        & \textcolor{red}{$2.5395 \pm 0.4520$}\\% [#4/#11]
		\textcolor{red}{Indep-HetGP}% [#4/#6]
		                                                        & \textcolor{red}{$1.2644 \pm 0.0993$}% [#4/#11]
		                                                        & \textcolor{red}{$0.5033 \pm 0.0099$}% [#4/#11]
		                                                        & \textcolor{red}{$\mathbf{1.8371 \pm 0.4420}$}\\% [#4/#11]
		\textcolor{red}{LMC-DGP}% [#6]
		                                                        & \textcolor{red}{$0.9917 \pm 0.1736$}% [#6/#11]
		                                                        & \textcolor{red}{$0.4924 \pm 0.0204$}% [#6/#11]
		                                                        & \textcolor{red}{$2.8493 \pm 0.4021$}\\% [#6/#11]
		\textcolor{red}{T-Atten}% [#7]
		                                                        & \textcolor{red}{$0.9615 \pm 0.1579$}% [#7/#11]
		                                                        & \textcolor{red}{$0.4988 \pm 0.0112$}% [#7/#11]
		                                                        & \textcolor{red}{$3.2892 \pm 0.5239$}\\% [#7/#11]
		\midrule
		\textcolor{red}{BO-qParEGO}% [#9]
		                                                        & \textcolor{red}{$1.1658 \pm 0.3092$}% [#9/#11]
		                                                        & \textcolor{red}{$0.5604 \pm 0.0432$}% [#9/#11]
		                                                        & \textcolor{red}{$2.5219 \pm 1.4452$}\\% [#9/#11]
		\textcolor{red}{BO-qEHVI}% [#9]
		                                                        & \textcolor{red}{$0.8116 \pm 0.0504$}% [#9/#11]
		                                                        & \textcolor{red}{$0.5369 \pm 0.0171$}% [#9/#11]
		                                                        & \textcolor{red}{$4.3638 \pm 0.6261$}\\% [#9/#11]
		\textcolor{red}{BO-qNEHVI}% [#9]
		                                                        & \textcolor{red}{$0.7907 \pm 0.0502$}% [#9/#11]
		                                                        & \textcolor{red}{$0.5337 \pm 0.0075$}% [#9/#11]
		                                                        & \textcolor{red}{$4.3390 \pm 0.6560$}\\% [#9/#11]
		\bottomrule
	\end{tabular*}
\end{table}


Tables 4-5 summarize the statistical performance and QL over 20 independent runs. \textcolor{red}{Under the prespecified selection rule, the selected optimal solutions settle in a narrow band: robust throughput between $5.88$ and $6.11$\,Mbps, robust BER between $0.2160$ and $0.2186$, and robust PAPR between $7.85$ and $8.58$\,dB. DA-DGP attains the highest robust throughput and the lowest QL$_{\text{Throughput}}$ among the ten one-shot Gaussian process surrogates, indicating that its dual-attention Pareto set approaches the aggressive $7.0$\,Mbps target more closely than the other one-shot surrogates. The sequentially augmented MOBO baselines BO-qEHVI and BO-qNEHVI explore a nearby high-throughput band, but they do so with substantially higher PAPR cost (QL$_{\text{PAPR}}\!\approx\!4.3$--$4.4$ vs DA-DGP $3.47$) and through an iterative acquisition loop that requires additional simulator calls; in contrast, DA-DGP exposes the high-throughput shoulder in a single surrogate fit.}% [#4/#6/#7/#9/#11] The role of DA-DGP within this band is characterised below from three angles:

Tables 4-5 总结了 20 次独立运行下的统计性能和 QL。\textcolor{red}{在预设选解规则下，所选最优解落在较窄的区间：鲁棒吞吐量 $5.88$ 至 $6.11$\,Mbps，鲁棒 BER $0.2160$ 至 $0.2186$，鲁棒 PAPR $7.85$ 至 $8.58$\,dB。DA-DGP 在十种一次性高斯过程代理中取得最大鲁棒吞吐量与最小 QL$_{\text{Throughput}}$，说明其双注意力 Pareto 集相比其他一次性代理更接近 $7.0$\,Mbps 的激进目标。序贯式加点 MOBO 基线 BO-qEHVI 与 BO-qNEHVI 也探索到相邻高吞吐区间，但伴随显著更高的 PAPR 代价（QL$_{\text{PAPR}}\!\approx\!4.3$--$4.4$，DA-DGP 为 $3.47$），并需要额外序贯调用仿真器进行迭代采集；相比之下，DA-DGP 通过一次性代理拟合即可暴露高吞吐肩部。}% [#4/#6/#7/#9/#11] DA-DGP 在该区间内的定位从以下三个角度刻画：

\textcolor{red}{First, DA-DGP delivers the most throughput-oriented selected solution among the one-shot surrogates. Its selected optimal solution achieves a robust throughput of $6.0526$\,Mbps with the smallest standard deviation among shared two-layer surrogate baselines ($\pm 0.0815$), exceeding every weighted-multitask scheme and every independent/coregionalized GP baseline on this response. The two iterative BO acquisitions occupy the same high-throughput regime only after their sequential acquisition loops, whereas DA-DGP reaches the $6.05$\,Mbps shoulder without additional simulator-driven augmentation. Indep-DGP and Indep-HetGP appear more conservative because their narrower Pareto sets cluster on the low-throughput slope of the front and therefore do not reach the shoulder exposed by the dual-attention front.}% [#4/#6/#8/#9]

\textcolor{red}{首先，在一次性代理中 DA-DGP 给出了最偏向吞吐提升的所选最优解。其所选最优解的鲁棒吞吐量为 $6.0526$\,Mbps（$\pm 0.0815$，在共享两层结构的代理基线中标准差最小），在该响应上高于全部加权多任务方案及独立/coregionalized GP 基线。两种迭代式 BO acquisition 需要通过序贯采集循环才能进入相同高吞吐区间，而 DA-DGP 无需额外仿真器加点即可触及 $6.05$\,Mbps 高吞吐肩部。Indep-DGP 和 Indep-HetGP 表现得更保守，是因为其 Pareto 集较窄、聚集在前沿的低吞吐段，因此未能触及双注意力前沿暴露出的肩部区域。}% [#4/#6/#8/#9]

\textcolor{red}{Second, the per-objective quality loss in Table~5 supports the same interpretation. DA-DGP achieves the smallest QL$_{\text{Throughput}}=0.9086\pm0.1677$ among one-shot Gaussian process surrogates, ahead of every weighted-multitask baseline (EQUAL $0.9856$, MGDA $0.9599$, UW $1.0242$, DWA $1.0371$), the pure shared DGP without attention ($0.9666$), and all independent GP variants (Indep-DGP $0.9958$, Indep-HetGP $1.2644$). In other words, the dual-attention Pareto set is closer to the throughput target, rather than simply filling the interior of the front. Indep-HetGP's smaller QL$_{\text{PAPR}}$ is associated with not reaching the high-throughput region of the design space, and BO-qEHVI/qNEHVI's smaller QL$_{\text{Throughput}}$ ($0.81$--$0.79$) likewise comes with the largest QL$_{\text{PAPR}}$ in the table ($4.3$--$4.4$), reflecting the same throughput--PAPR physical coupling.}% [#4/#6/#7/#9]

\textcolor{red}{其次，Table~5 中按目标分解的质量损失给出一致解释。DA-DGP 在一次性高斯过程代理中取得最小的 QL$_{\text{Throughput}}=0.9086\pm0.1677$，优于所有加权多任务基线（EQUAL $0.9856$、MGDA $0.9599$、UW $1.0242$、DWA $1.0371$）、不含注意力的共享 DGP（$0.9666$），以及全部独立 GP 变体（Indep-DGP $0.9958$、Indep-HetGP $1.2644$）。换言之，双注意力 Pareto 集更接近吞吐目标，而不仅仅是填充前沿内部。Indep-HetGP 的 QL$_{\text{PAPR}}$ 更小，与其未触及设计空间高吞吐区有关；BO-qEHVI / BO-qNEHVI 的 QL$_{\text{Throughput}}$ 更小（$0.81$--$0.79$）则同样伴随表中最高的 QL$_{\text{PAPR}}$（$4.3$--$4.4$），反映的也是同一吞吐-PAPR 物理耦合。}% [#4/#6/#7/#9]

\textcolor{red}{Third, the run-to-run dispersion of DA-DGP on the three responses ($\pm 0.0815$ on throughput, $\pm 0.0008$ on BER and $\pm 0.1348$ on PAPR) is among the smallest of the shared two-layer surrogate baselines, indicating that the dual-attention training does not magnify Pareto drift. The remaining differences across methods are amplified at the Pareto-set level (HV, IGD, TOPSIS), which motivates the four-objective robust analysis reported next.}% [#8/#11]

\textcolor{red}{第三，DA-DGP 在三个响应上的运行间离散度（$\pm 0.0815$ 吞吐量、$\pm 0.0008$ BER、$\pm 0.1348$ PAPR）位于共享两层结构的代理基线中较小一档，表明双注意力训练并未放大 Pareto 漂移。各方法的细微差距在 Pareto 集层面（HV、IGD、TOPSIS）会被进一步放大，从而引出下文的四目标鲁棒分析。}% [#8/#11]

\textcolor{red}{\paragraph{Robust four-objective Pareto analysis.}}% [#8]
\textcolor{red}{At the Pareto-frontier level, the $K=20$ NSGA-II runs of each method jointly produce $\sim\!400$ non-dominated candidates. Each candidate is first evaluated by the OFDM waveform simulator over $R=16$ noise realizations to obtain $(\hat{m}_{i},\hat{v}_{i})$ for throughput, BER and PAPR; the fourth objective, energy efficiency $y_{4}=1/E_{b}$, is then derived by the separate link-budget post-processing model from transmit power and robust throughput.}% [#2/#8]
\textcolor{red}{Table~7 summarizes the robust frontier of all thirteen methods--ten Gaussian process surrogate variants together with three multi-objective Bayesian optimization (MOBO) baselines--through nine standard Pareto indicators: the number of within-method Pareto points (\#P), the number of contributions to the global front (\#GP), the global contribution rate (GC), the normalized hypervolume ($\mathrm{HV}_{\mathrm{n}}$), the hypervolume ratio (HVR), the inverted generational distance (IGD), the inverted generational distance plus (IGD$^{+}$), the spacing metric (SP), and the closest-ideal distance (CID). The nine indicators are aggregated by an AHP-weighted TOPSIS to give the closeness coefficient $C_{i}$ and the overall rank.}% [#8/#11]
\textcolor{red}{As shown in Table~7, DA-DGP attains $\mathrm{HV}_{\mathrm{n}}=0.3326$, $\mathrm{HVR}=0.7764$, $\mathrm{IGD}^{+}=0.0401$ and the highest closeness $C_{i}=0.9322$, ranking ahead of Equal ($C_{i}=0.9272$) and BO-qNEHVI ($C_{i}=0.9262$). Among the one-shot GP surrogates, DA-DGP leads on hypervolume-based indicators and within-method frontier density, whereas Equal and LMC-DGP remain close and are slightly better on selected distance-only indicators such as closest-ideal distance (CID). Therefore, DA-DGP gives the highest integrated robust-frontier rank under the stated AHP-TOPSIS aggregation, while not claiming dominance on every individual Pareto indicator.}% [#4/#6/#8/#11]
\textcolor{red}{Even without additional sequential simulator calls, DA-DGP reaches an $\mathrm{HV}_{\mathrm{n}}$ value essentially tied with BO-qNEHVI, the hypervolume-aware BO competitor with the highest $\mathrm{HV}_{\mathrm{n}}$ in this comparison ($0.3326$ vs $0.3330$), and higher than the other two BO baselines (BO-qEHVI $0.3294$ and BO-qParEGO $0.1883$). It attains the highest TOPSIS rank under the stated aggregation by supplying more within-method Pareto candidates (\#P $=19.3$ vs $17.8$), a more uniform spacing metric (SP) relative to BO-qNEHVI ($0.0668$ vs $0.1083$), and a tighter closest-ideal distance (CID) than BO-qNEHVI ($0.7673$ vs $0.8007$); under this aggregation, the resulting frontier is denser and better balanced.}% [#8/#9]
\textcolor{red}{Figure~5 visualises the corresponding risk, four-objective, manifold and attainment views used to interpret these robust Pareto indicators.}% [#8/#11]

\textcolor{red}{\paragraph{鲁棒四目标 Pareto 分析。}}% [#8]
\textcolor{red}{在 Pareto 前沿层面，每种方法 $K=20$ 次 NSGA-II 运行共产生约 $400$ 个非支配候选解；每个候选解首先由 OFDM 波形仿真器在 $R=16$ 个噪声实现下评估，得到吞吐量、BER 与 PAPR 的 $(\hat{m}_{i},\hat{v}_{i})$，第四个目标能量效率 $y_{4}=1/E_{b}$ 则由独立链路预算后处理模型根据发射功率与稳健吞吐量进一步派生。}% [#2/#8]
\textcolor{red}{Table~7 通过九个标准 Pareto 指标——方法内 Pareto 点数 (\#P)、对全局前沿的贡献数 (\#GP)、全局贡献率 (GC)、归一化超体积 ($\mathrm{HV}_{\mathrm{n}}$)、超体积比 (HVR)、反世代距离 (IGD)、增强反世代距离 (IGD$^{+}$)、spacing metric (SP) 以及最近理想点距离 (CID)——汇总十三种方法的鲁棒前沿，其中包含十种基于 DGP 的变体以及三种多目标贝叶斯优化（MOBO）基线。九个指标通过 AHP 加权 TOPSIS 汇聚为接近度系数 $C_{i}$ 与综合排名。}% [#8/#11]
\textcolor{red}{由 Table~7 可见，DA-DGP 取得 $\mathrm{HV}_{\mathrm{n}}=0.3326$、$\mathrm{HVR}=0.7764$、$\mathrm{IGD}^{+}=0.0401$ 与最高的接近度 $C_{i}=0.9322$，排名高于 Equal（$C_{i}=0.9272$）与 BO-qNEHVI（$C_{i}=0.9262$）。在一次性 GP 代理中，DA-DGP 在超体积类指标和方法内前沿密度上领先；同时，Equal 与 LMC-DGP 在个别纯距离指标（如最近理想点距离 (CID)）上接近或略优。因此，在既定 AHP-TOPSIS 聚合规则下，DA-DGP 给出最强的综合鲁棒前沿结果，但并不声称其在每个单项 Pareto 指标上均逐项支配。}% [#4/#6/#8/#11]
\textcolor{red}{在无需额外序贯调用仿真器的情况下，DA-DGP 的 $\mathrm{HV}_{\mathrm{n}}$ 与最强的超体积感知 BO 方法 BO-qNEHVI 基本持平（$0.3326$ vs $0.3330$），并高于另外两种 BO 基线（BO-qEHVI $0.3294$、BO-qParEGO $0.1883$）。同时，DA-DGP 凭借更多的方法内 Pareto 候选（\#P $=19.3$ vs $17.8$）、相对 BO-qNEHVI 更均匀的 spacing metric (SP)（$0.0668$ vs $0.1083$）以及更紧的最近理想点距离 (CID)（$0.7673$ vs $0.8007$）保持 TOPSIS 综合第一；在该聚合规则下，其前沿更密集、更均衡。}% [#8/#9]
\textcolor{red}{对应的风险、四目标、流形与达成概率视角见 Figure~5，用于解释这些鲁棒 Pareto 指标。}% [#8/#11]

\begin{table}[!htbp]
	\centering
	\caption{Robust four-objective Pareto indicators of all 13 methods.}
	\caption*{13 种方法的鲁棒四目标 Pareto 指标。}
	\caption*{\textcolor{red}{13 种方法的鲁棒四目标 Pareto 指标汇总。数值为 $K=20$ 次 NSGA-II 运行的均值，每个候选解在 $R=16$ 个噪声实现下评估；TOPSIS 排名按 Section~4.2.4 所述 AHP 权重计算。}}% [#4/#6/#8/#9/#11]
	\label{tab:robust_pareto}
	\resizebox{\textwidth}{!}{%
	\begin{tabular}{lccccccccccc}
		\toprule
		\textbf{Method} & \textbf{\#P} & \textbf{\#GP} & \textbf{GC} & \textbf{HV$_{\text{n}}$} & \textbf{HVR} & \textbf{IGD} & \textbf{IGD$^{+}$} & \textbf{Spacing} & \textbf{ClosestIdeal} & \textbf{$C_{i}$} & \textbf{Rank}\\
		\midrule
		\textcolor{red}{\textbf{DA-DGP}} & \textcolor{red}{$\mathbf{19.3}$} & \textcolor{red}{$14.2$} & \textcolor{red}{$0.080$} & \textcolor{red}{$0.3326$} & \textcolor{red}{$0.7764$} & \textcolor{red}{$0.0743$} & \textcolor{red}{$0.0401$} & \textcolor{red}{$0.0668$} & \textcolor{red}{$0.7673$} & \textcolor{red}{$\mathbf{0.9322}$} & \textcolor{red}{$\mathbf{1}$}\\% [#8]
		\textcolor{red}{Equal}          & \textcolor{red}{$19.1$} & \textcolor{red}{$14.0$} & \textcolor{red}{$0.078$} & \textcolor{red}{$0.3316$} & \textcolor{red}{$0.7741$} & \textcolor{red}{$0.0747$} & \textcolor{red}{$0.0399$} & \textcolor{red}{$0.0579$} & \textcolor{red}{$\mathbf{0.7605}$} & \textcolor{red}{$0.9272$} & \textcolor{red}{$2$}\\% [#8/#11]
		\textcolor{red}{BO-qNEHVI} & \textcolor{red}{$17.8$} & \textcolor{red}{$16.4$} & \textcolor{red}{$0.092$} & \textcolor{red}{$\mathbf{0.3330}$} & \textcolor{red}{$\mathbf{0.7774}$} & \textcolor{red}{$0.0765$} & \textcolor{red}{$\mathbf{0.0367}$} & \textcolor{red}{$0.1083$} & \textcolor{red}{$0.8007$} & \textcolor{red}{$0.9262$} & \textcolor{red}{$3$}\\% [#9]
		\textcolor{red}{MGDA}           & \textcolor{red}{$19.2$} & \textcolor{red}{$14.2$} & \textcolor{red}{$0.080$} & \textcolor{red}{$0.3219$} & \textcolor{red}{$0.7514$} & \textcolor{red}{$0.0752$} & \textcolor{red}{$0.0416$} & \textcolor{red}{$0.0713$} & \textcolor{red}{$0.7888$} & \textcolor{red}{$0.9189$} & \textcolor{red}{$4$}\\% [#8]
		\textcolor{red}{DWA}            & \textcolor{red}{$18.9$} & \textcolor{red}{$13.8$} & \textcolor{red}{$0.078$} & \textcolor{red}{$0.3263$} & \textcolor{red}{$0.7617$} & \textcolor{red}{$0.0739$} & \textcolor{red}{$0.0412$} & \textcolor{red}{$0.0620$} & \textcolor{red}{$0.7740$} & \textcolor{red}{$0.9170$} & \textcolor{red}{$5$}\\% [#8]
		\textcolor{red}{T-Atten} & \textcolor{red}{$18.9$} & \textcolor{red}{$13.7$} & \textcolor{red}{$0.077$} & \textcolor{red}{$0.3230$} & \textcolor{red}{$0.7540$} & \textcolor{red}{$0.0737$} & \textcolor{red}{$0.0403$} & \textcolor{red}{$0.0627$} & \textcolor{red}{$0.7797$} & \textcolor{red}{$0.9163$} & \textcolor{red}{$6$}\\% [#7]
		\textcolor{red}{UW}             & \textcolor{red}{$18.9$} & \textcolor{red}{$13.1$} & \textcolor{red}{$0.074$} & \textcolor{red}{$0.3202$} & \textcolor{red}{$0.7474$} & \textcolor{red}{$0.0718$} & \textcolor{red}{$0.0411$} & \textcolor{red}{$0.0636$} & \textcolor{red}{$0.7716$} & \textcolor{red}{$0.9002$} & \textcolor{red}{$7$}\\% [#8]
		\textcolor{red}{BO-qEHVI} & \textcolor{red}{$17.6$} & \textcolor{red}{$16.2$} & \textcolor{red}{$0.091$} & \textcolor{red}{$0.3294$} & \textcolor{red}{$0.7690$} & \textcolor{red}{$0.0935$} & \textcolor{red}{$0.0419$} & \textcolor{red}{$0.1300$} & \textcolor{red}{$0.8019$} & \textcolor{red}{$0.8912$} & \textcolor{red}{$8$}\\% [#9]
		\textcolor{red}{LMC-DGP}        & \textcolor{red}{$18.7$} & \textcolor{red}{$12.6$} & \textcolor{red}{$0.071$} & \textcolor{red}{$0.3139$} & \textcolor{red}{$0.7327$} & \textcolor{red}{$0.0796$} & \textcolor{red}{$0.0457$} & \textcolor{red}{$0.0617$} & \textcolor{red}{$0.7630$} & \textcolor{red}{$0.8637$} & \textcolor{red}{$9$}\\% [#6]
		\textcolor{red}{Pure DGP}       & \textcolor{red}{$18.5$} & \textcolor{red}{$12.0$} & \textcolor{red}{$0.068$} & \textcolor{red}{$0.3201$} & \textcolor{red}{$0.7471$} & \textcolor{red}{$0.0770$} & \textcolor{red}{$0.0443$} & \textcolor{red}{$0.0636$} & \textcolor{red}{$0.7834$} & \textcolor{red}{$0.8609$} & \textcolor{red}{$10$}\\% [#7]
		\textcolor{red}{Indep-DGP}      & \textcolor{red}{$19.1$} & \textcolor{red}{$15.2$} & \textcolor{red}{$0.085$} & \textcolor{red}{$0.2823$} & \textcolor{red}{$0.6594$} & \textcolor{red}{$0.0877$} & \textcolor{red}{$0.0563$} & \textcolor{red}{$0.0633$} & \textcolor{red}{$0.7986$} & \textcolor{red}{$0.7927$} & \textcolor{red}{$11$}\\% [#4]
		\textcolor{red}{Indep-HetGP}    & \textcolor{red}{$17.1$} & \textcolor{red}{$7.5$} & \textcolor{red}{$0.042$} & \textcolor{red}{$0.2664$} & \textcolor{red}{$0.6216$} & \textcolor{red}{$0.1240$} & \textcolor{red}{$0.0842$} & \textcolor{red}{$0.0663$} & \textcolor{red}{$0.7843$} & \textcolor{red}{$0.4930$} & \textcolor{red}{$12$}\\% [#4]
		\textcolor{red}{BO-qParEGO}     & \textcolor{red}{$17.9$} & \textcolor{red}{$14.9$} & \textcolor{red}{$0.084$} & \textcolor{red}{$0.1883$} & \textcolor{red}{$0.4392$} & \textcolor{red}{$0.2598$} & \textcolor{red}{$0.1343$} & \textcolor{red}{$0.0987$} & \textcolor{red}{$0.9081$} & \textcolor{red}{$0.1807$} & \textcolor{red}{$13$}\\% [#9]
		\bottomrule
	\end{tabular}}
\end{table}

\textcolor{red}{Figure~5 examines the robust Pareto evidence from four complementary views. Subfigure (a) places the method-level Pareto candidates on the normalized objective-cost--variance-risk plane; the DA-DGP points trace the lower-risk efficient boundary and occupy the low-risk portion of the trade-off curve, indicating that the dual-attention front remains robust across noise realizations. Subfigure (b) gives the four-objective bubble view, where throughput and BER form the axes, PAPR is encoded by color and energy efficiency by bubble size; DA-DGP remains on the main front and covers the high-throughput, low-BER shoulder. Subfigure (c) embeds all candidates into the objective manifold and shows DA-DGP overlapping the global Pareto manifold across the dominant front. Subfigure (d) reports the attainment-probability map in the (BER, throughput) plane, where DA-DGP follows the high-attainment ridge, suggesting that its front repeatedly falls in regions with high global Pareto hit rates.}% [#8/#9/#11]

\textcolor{red}{Figure~5 从四个互补视角刻画鲁棒 Pareto 证据。子图 (a) 将方法内 Pareto 候选置于归一化目标代价--方差风险平面，DA-DGP 点沿着低风险有效边界分布，并占据 trade-off 曲线的低风险部分，表明双注意力前沿在噪声 realization 下仍保持较好的稳健性。子图 (b) 为四目标气泡图，坐标轴为 throughput 与 BER，颜色编码 PAPR，气泡大小编码能量效率；DA-DGP 保持在主前沿上，并覆盖高吞吐、低 BER 的关键肩部。子图 (c) 将所有候选嵌入目标流形，显示 DA-DGP 在主导前沿上与全局 Pareto manifold 持续重合。子图 (d) 给出 (BER, throughput) 平面的达成概率图，DA-DGP 沿高达成概率脊线展开，说明其前沿反复落在全局 Pareto 命中率较高的区域。}% [#8/#9/#11]

\begin{figure}[!htbp]
	\centering
	\begin{subfigure}[b]{0.45\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG5(a).pdf}
		\caption{\textcolor{red}{Uncertainty-risk map.}}% [#8/#11]
		\caption*{\textcolor{red}{不确定性风险图。}}% [#8/#11]
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.45\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG5(b).pdf}
		\caption{\textcolor{red}{Four-objective bubble view.}}% [#8/#11]
		\caption*{\textcolor{red}{四目标气泡视图。}}% [#8/#11]
	\end{subfigure}

	\par\vspace{0.35\baselineskip}

	\begin{subfigure}[b]{0.45\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG5(c).pdf}
		\caption{\textcolor{red}{Objective manifold embedding.}}% [#8/#11]
		\caption*{\textcolor{red}{目标流形嵌入。}}% [#8/#11]
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.45\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG5(d).pdf}
		\caption{\textcolor{red}{Attainment probability map.}}% [#8/#11]
		\caption*{\textcolor{red}{达成概率图。}}% [#8/#11]
	\end{subfigure}
	\caption{\textcolor{red}{Robust four-objective Pareto visualisations selected to highlight DA-DGP's risk control, four-objective trade-off, manifold coverage and attainment probability.}}% [#8/#11]
	\caption*{\textcolor{red}{用于突出 DA-DGP 风险控制、四目标 trade-off、流形覆盖和达成概率的鲁棒四目标 Pareto 可视化。}}% [#8/#11]
	\label{fig:pareto_visualisation}
\end{figure}


\textcolor{red}{\paragraph{Comparison with Bayesian optimization.}}% [#9]
\textcolor{red}{As a contrasting class of multi-objective surrogate-assisted optimization, three representative MOBO methods--qParEGO, qEHVI and qNEHVI~\citep{Daulton2020,Daulton2021}--are evaluated under the same initial $400$-sample design used by the GP surrogates and are then given a $20$-step sequential acquisition budget in each of the $20$ runs before evaluation by the same four-objective robust simulator.}% [#9/#11]
\textcolor{red}{Table~7 shows that BO-qNEHVI is the highest-ranked MOBO competitor and gives a hypervolume value very close to DA-DGP, consistent with the known efficiency of hypervolume-aware acquisition; qParEGO, in contrast, is hurt by scalarization bias and degrades to $\mathrm{IGD}=0.2598$ and an overall closeness of $C_{i}=0.1807$ (rank $13$).}% [#9]
\textcolor{red}{DA-DGP converts a one-shot surrogate fit into the highest integrated robust Pareto rank under the stated AHP-TOPSIS assessment in this study: without iterative acquisition or model retraining, it delivers a denser and better balanced robust frontier across all surrogate and MOBO baselines.}% [#9]

\textcolor{red}{\paragraph{与贝叶斯优化的对比。}}% [#9]
\textcolor{red}{作为多目标代理辅助优化的另一类代表，三种 MOBO 方法 qParEGO、qEHVI 与 qNEHVI~\citep{Daulton2020,Daulton2021} 使用与 GP 代理相同的 $400$ 个初始设计样本进行评估，并在 $20$ 次运行中分别给予每次 $20$ 步的序贯采集预算，随后再由相同的四目标鲁棒仿真器评估。}% [#9/#11]
\textcolor{red}{Table~7 显示，BO-qNEHVI 是最强的 MOBO 竞争者，其超体积结果与 DA-DGP 非常接近，与超体积感知采集函数已知的效率相一致；与此对照，qParEGO 受标量化偏差影响，IGD 退化至 $0.2598$，综合接近度为 $C_{i}=0.1807$（排名第 $13$）。}% [#9]
\textcolor{red}{DA-DGP 则在本研究中将一次性代理拟合转化为最高的综合鲁棒 Pareto 排名：无需迭代采样或重训练，即可在全部代理与 MOBO 基线的 AHP-TOPSIS 评价下提供密度更高、综合更均衡的鲁棒前沿。}% [#9]

\textcolor{red}{\paragraph{Training cost and time complexity.}}% [#5]
\textcolor{red}{For the shared two-layer sparse variational DGP backbone, the dominant per-batch cost is}% [#5]
\[
\textcolor{red}{\mathcal{O}\!\left((h+p)(BM^{2}+M^{3})\right),}
\]
\textcolor{red}{where $h$ is the hidden latent dimension, $p$ is the number of response tasks, $B$ is the mini-batch size and $M$ is the number of inducing points. Over $E$ epochs, the corresponding total training order is}% [#5]
\[
\textcolor{red}{\mathcal{O}\!\left(E\lceil N/B\rceil(h+p)(BM^{2}+M^{3})\right).}
\]
\textcolor{red}{DA-DGP keeps this same asymptotic order as the shared DGP backbone, but its bilevel update in Eqs.~(24)--(25) introduces a constant-factor wall-clock overhead from one virtual update, one validation backward pass and two finite-difference Hessian--vector evaluations.}% [#5]
\textcolor{red}{In the OFDM experiment ($N=400$, $B=128$, $h=5$, $p=3$, $M=128$, $E=500$), DA-DGP requires $397.79$\,s in total ($0.7956$\,s/epoch), compared with $100.57$\,s for Equal, $99.56$\,s for Pure DGP, $164.87$\,s for MGDA and $163.40$\,s for Indep-DGP. Thus, the extra cost is measurable but remains practical for a one-shot surrogate fit, especially because it achieves the top integrated robust Pareto rank in Table~7 without the iterative acquisition loop required by sequential MOBO. Detailed wall-clock measurements and the hardware/software stack are reported in Appendix~F of the supplementary material.}% [#5/#9]

\textcolor{red}{\paragraph{训练代价与时间复杂度。}}% [#5]
\textcolor{red}{对于共享两层 sparse variational DGP 主干，每个 batch 的主导计算量为}% [#5]
\[
\textcolor{red}{\mathcal{O}\!\left((h+p)(BM^{2}+M^{3})\right),}
\]
\textcolor{red}{其中 $h$ 为隐藏潜在维度，$p$ 为响应任务数，$B$ 为 mini-batch size，$M$ 为 inducing points 数量。训练 $E$ 个 epochs 的总复杂度为}% [#5]
\[
\textcolor{red}{\mathcal{O}\!\left(E\lceil N/B\rceil(h+p)(BM^{2}+M^{3})\right).}
\]
\textcolor{red}{DA-DGP 与共享 DGP 主干保持相同渐近阶，但 Eqs.~(24)--(25) 中的 bilevel 更新会因一次虚拟更新、一次验证集反向传播以及两次 finite-difference Hessian--vector 计算带来常数因子的 wall-clock 开销。}% [#5]
\textcolor{red}{在 OFDM 实验中（$N=400$、$B=128$、$h=5$、$p=3$、$M=128$、$E=500$），DA-DGP 总训练耗时为 $397.79$\,s（$0.7956$\,s/epoch），而 Equal 为 $100.57$\,s，Pure DGP 为 $99.56$\,s，MGDA 为 $164.87$\,s，Indep-DGP 为 $163.40$\,s。因此，DA-DGP 的额外计算开销是可测的，但对于一次性代理拟合仍具有实际可接受性，尤其是其无需序贯 MOBO 的迭代采集过程，即可在 Table~7 中取得最高综合鲁棒 Pareto 排名。具体 wall-clock 测量值与软硬件环境见补充材料 Appendix~F。}% [#5/#9]






\begin{table}[!htbp]
	\centering
	\caption{Statistical summary of received EVM for the selected optimal solutions over 20 runs (dB).}
	\caption*{20 次运行中所选最优解的接收 EVM（dB）统计汇总。}
	\label{tab:evm_stats}
	\small
	\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}lccccc}
		\toprule
		\textbf{Method} & \textbf{Mean $\pm$ Std} & \textbf{Median} & \textbf{Min} & \textbf{Max} & \textbf{IQR} \\
		\midrule
		\textbf{DA-DGP}        & \textcolor{red}{$\mathbf{11.4648 \pm 1.0419}$}% [#11]
		                       & \textcolor{red}{$11.1877$} & \textcolor{red}{$10.3291$} & \textcolor{red}{$14.4808$} & \textcolor{red}{$0.9839$} \\
		EQUAL                  & \textcolor{red}{$11.9124 \pm 1.6627$}% [#11]
		                       & \textcolor{red}{$11.2414$} & \textcolor{red}{$10.5439$} & \textcolor{red}{$17.4231$} & \textcolor{red}{$1.6107$} \\
		\textcolor{red}{Pure DGP}% [#7]
		                       & \textcolor{red}{$11.6967 \pm 1.1421$}% [#7/#11]
		                       & \textcolor{red}{$11.5229$} & \textcolor{red}{$10.3227$} & \textcolor{red}{$14.8353$} & \textcolor{red}{$1.3989$} \\
		DWA                    & \textcolor{red}{$11.6101 \pm 0.8044$}% [#11]
		                       & \textcolor{red}{$11.2387$} & \textcolor{red}{$10.7373$} & \textcolor{red}{$13.3239$} & \textcolor{red}{$1.1043$} \\
		UW                     & \textcolor{red}{$11.8394 \pm 1.1318$}% [#11]
		                       & \textcolor{red}{$11.8410$} & \textcolor{red}{$9.3768$} & \textcolor{red}{$14.6181$} & \textcolor{red}{$1.1285$} \\
		MGDA                   & \textcolor{red}{$11.7692 \pm 1.1493$}% [#11]
		                       & \textcolor{red}{$11.5032$} & \textcolor{red}{$10.1646$} & \textcolor{red}{$14.2999$} & \textcolor{red}{$1.4995$} \\
		\textcolor{red}{Indep-DGP}% [#4/#6]
		                       & \textcolor{red}{$12.5140 \pm 2.0350$}% [#4/#11]
		                       & \textcolor{red}{$11.8887$} & \textcolor{red}{$10.6884$} & \textcolor{red}{$18.1743$} & \textcolor{red}{$1.9075$} \\
		\textcolor{red}{Indep-HetGP}% [#4/#6]
		                       & \textcolor{red}{$11.5661 \pm 0.6530$}% [#4/#11]
		                       & \textcolor{red}{$11.4567$} & \textcolor{red}{$10.6837$} & \textcolor{red}{$13.6582$} & \textcolor{red}{$0.4501$} \\
		\textcolor{red}{LMC-DGP}% [#6]
		                       & \textcolor{red}{$12.8171 \pm 2.7213$}% [#6/#11]
		                       & \textcolor{red}{$11.8866$} & \textcolor{red}{$10.1993$} & \textcolor{red}{$20.8251$} & \textcolor{red}{$2.5701$} \\
		\textcolor{red}{T-Atten}% [#7]
		                       & \textcolor{red}{$11.7811 \pm 2.1049$}% [#7/#11]
		                       & \textcolor{red}{$11.3261$} & \textcolor{red}{$10.5054$} & \textcolor{red}{$20.0557$} & \textcolor{red}{$0.9608$} \\
		\midrule
		\textcolor{red}{BO-qParEGO}% [#9]
		                       & \textcolor{red}{$11.8696 \pm 1.5517$}% [#9/#11]
		                       & \textcolor{red}{$11.4693$} & \textcolor{red}{$10.6413$} & \textcolor{red}{$17.1338$} & \textcolor{red}{$0.8466$} \\
		\textcolor{red}{BO-qEHVI}% [#9]
		                       & \textcolor{red}{$11.3915 \pm 0.4223$}% [#9/#11]
		                       & \textcolor{red}{$11.4266$} & \textcolor{red}{$10.4812$} & \textcolor{red}{$12.2190$} & \textcolor{red}{$0.3422$} \\
		\textcolor{red}{BO-qNEHVI}% [#9]
		                       & \textcolor{red}{$11.3816 \pm 0.3678$}% [#9/#11]
		                       & \textcolor{red}{$11.3158$} & \textcolor{red}{$10.8346$} & \textcolor{red}{$12.0891$} & \textcolor{red}{$0.3314$} \\
		\bottomrule
	\end{tabular*}
\end{table}
\vspace{-0.5em}

\begin{figure}[!htbp]
	\centering
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(a).pdf}
		\caption{DA-DGP}
		\label{fig:const_dadgp}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(b).pdf}
		\caption{Equal}
		\label{fig:const_equal}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(c).pdf}
		\caption{Pure DGP}
		\label{fig:const_puredgp}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(d).pdf}
		\caption{DWA}
		\label{fig:const_dwa}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(e).pdf}
		\caption{UW}
		\label{fig:const_uw}
	\end{subfigure}

	\par\vspace{0.15em}

	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(f).pdf}
		\caption{MGDA}
		\label{fig:const_mgda}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(g).pdf}
		\caption{Indep-DGP}
		\label{fig:const_indepdgp}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(h).pdf}
		\caption{Indep-HetGP}
		\label{fig:const_indephetgp}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(i).pdf}
		\caption{LMC-DGP}
		\label{fig:const_lmcdgp}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(j).pdf}
		\caption{\textcolor{red}{T-Atten}}% [#7]
		\label{fig:const_tatten}
	\end{subfigure}

	\par\vspace{0.15em}

	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(k).pdf}
		\caption{BO-qParEGO}
		\label{fig:const_boqparego}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(l).pdf}
		\caption{BO-qEHVI}
		\label{fig:const_boqehvi}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.185\textwidth}
		\centering
		\includegraphics[width=\textwidth]{FIG6(m).pdf}
		\caption{BO-qNEHVI}
		\label{fig:const_boqnehvi}
	\end{subfigure}
	\hfill
	\hfill\null
	\caption{\textcolor{red}{Constellation diagrams for all 13 methods in the OFDM case.}}% [#7/#9/#11]
	\caption*{\textcolor{red}{OFDM 案例中 13 种方法的星座图。}}% [#7/#9/#11]
	\label{fig:constellation_diagrams}
\end{figure}

Figure~6 displays the constellation diagrams of the received signal on the complex plane, which provide a direct physical-layer check of link quality. \textcolor{red}{All thirteen methods are included in the main text, covering the proposed DA-DGP, static and dynamic weighting baselines, independent/coregionalized GP variants, the T-Atten ablation, and three sequential MOBO baselines. The DA-DGP symbols (Fig.~6a) form a compact, well-separated 16-QAM grid and remain visually comparable to the high-performing sequential BO panels (Fig.~6l--m), while avoiding the iterative acquisition loop required by BO. The contrast with T-Atten (Fig.~6j), Indep-DGP (Fig.~6g) and LMC-DGP (Fig.~6i) shows that removing sample-level attention or weakening the shared representation is associated with broader constellation clouds and heavier excursions.}% [#7/#9] \textcolor{red}{Table~6 reports the received signal quality of the same selected optimal solutions used in Tables~4--5; for each method, one optimal solution is selected from each run and subjected to a common physical-layer EVM check.}% [#9/#11] \textcolor{red}{Under this selected-solution EVM check, DA-DGP attains a mean EVM of $11.4648\pm1.0419$\,dB, the lowest among all one-shot Gaussian process surrogates and in the same narrow high-quality range as the sequentially augmented BO baselines (BO-qNEHVI $11.3816$\,dB, BO-qEHVI $11.3915$\,dB). DA-DGP also keeps the selected-solution spread tightly bounded (max $14.48$\,dB), whereas Indep-DGP, LMC-DGP and T-Atten exhibit maxima well above $18$\,dB, indicating that dual-attention training reduces tail excursions in the selected optimal-solution constellation patterns while preserving the efficiency of one-shot surrogate fitting.}% [#7/#9/#11] This suggests that the optimized parameter configuration (such as precise power allocation and pre-equalization strategies) effectively compensates for nonlinear channel distortions caused by multipath fading and carrier frequency offset.

Figure~6 展示了接收信号在复平面上的星座图，可作为通信链路物理层质量的直接检查。\textcolor{red}{正文展示全部 13 种方法，包括所提 DA-DGP、静态与动态加权基线、独立/协同 GP 变体、T-Atten 消融以及三种序贯式 MOBO 基线。DA-DGP 恢复的符号（Fig.~6a）形成紧凑、清晰可分的 16-QAM 网格，在视觉上接近表现较好的序贯 BO 面板（Fig.~6l--m），但不需要 BO 的迭代采集过程。与 T-Atten（Fig.~6j）、Indep-DGP（Fig.~6g）和 LMC-DGP（Fig.~6i）的对比表明，移除样本级注意力或削弱共享表示会伴随更分散的星座云团和更明显的尾部偏移。}% [#7/#9] \textcolor{red}{Table~6 汇报与 Tables~4--5 相同的所选最优解的接收信号质量；对每种方法，每次运行选取一个最优解，并进行统一的物理层 EVM 检查。}% [#9/#11] \textcolor{red}{在这一所选最优解 EVM 检查中，DA-DGP 取得平均 EVM $11.4648\pm1.0419$\,dB，是所有一次性高斯过程代理中最低，并与序贯式加点 BO 基线处于同一优良区间（BO-qNEHVI $11.3816$\,dB、BO-qEHVI $11.3915$\,dB）。同时，DA-DGP 的所选最优解扩散范围较小（最大值 $14.48$\,dB），而 Indep-DGP、LMC-DGP 与 T-Atten 的最大 EVM 均高于 $18$\,dB，说明双注意力训练在保持一次性代理效率的同时降低了所选最优解星座图模式的尾部漂移。}% [#7/#9/#11] 这表明优化后的参数配置（如精确功率分配和预均衡策略）能够有效补偿由多径衰落和载波频偏引起的非线性信道失真。

Combining the numerical case study with the OFDM engineering case, the proposed dual-attention DA-DGP framework shows strong empirical performance for RPD problems characterized by \textcolor{red}{limited engineering samples}, high-dimensional nonlinearities, and multi-objective conflicts.% [#12] By compensating for channel impairments at the physical mechanism level and minimizing the bias and variance of quality loss at the statistical level, this method provides an efficient and robust technical path for intelligent resource scheduling and power control in 5G/6G communication systems.

综合数值案例和 OFDM 工程案例，所提出的双注意力 DA-DGP 框架在具有 \textcolor{red}{有限工程样本}、高维非线性和多目标冲突特征的 RPD 问题中表现出较强的实证性能。% [#12] 该方法在物理机制层面补偿信道损伤，并在统计层面最小化质量损失的 bias 和 variance，为 5G/6G 通信系统中的智能资源调度和功率控制提供了一条高效且稳健的技术路径。


\section{Conclusion}  \label{s:sec5}
A DA-DGP framework for the RPD of complex multi-output systems is proposed. The suggested algorithm takes advantage of an end-to-end weighted variational inference system, providing precise ROI focusing and adaptive task balancing by jointly optimizing dual-attention weights. The algorithm is shown empirically powerful in handling high-dimensional nonlinearities compared with benchmark methods, exhibited by the optimization of synthetic test functions. The proposed algorithm is applied to a high-fidelity 5G/6G OFDM simulation-based case study of optimizing communication link quality. \textcolor{red}{Despite the limited engineering sample budget, the algorithm consistently identifies selected solutions on the high-throughput shoulder of the robust Pareto front, keeps selected-solution EVM in the same high-quality range as the leading sequentially augmented MOBO baselines, and obtains the highest integrated robust Pareto rank among the one-shot Gaussian process surrogates.}% [#9/#11/#12] The inferior performances of the benchmark methods can be mainly attributed to the limitations of traditional ``mean-oriented" models and unmanaged gradient conflicts.

本文提出了一种用于复杂 multi-output 系统 RPD 的 DA-DGP 框架。所提算法利用端到端加权 variational inference 系统，通过联合优化双注意力权重，实现精确 ROI 聚焦和自适应任务平衡。基于合成测试函数优化结果，相较基准方法，该算法在处理高维非线性方面表现出显著经验优势。本文还将所提算法应用于基于高保真 5G/6G OFDM 仿真的通信链路质量优化案例研究。\textcolor{red}{尽管工程样本预算有限，该算法仍能稳定识别出位于鲁棒 Pareto 前沿高吞吐肩部的所选最优解，其所选最优解 EVM 与表现领先的序贯式加点 MOBO 基线处于同一优良区间，并在一次性高斯过程代理中取得最高综合鲁棒 Pareto 排名。}% [#9/#11/#12] 基准方法表现较差主要可归因于传统``mean-oriented''模型的局限以及未被管理的梯度冲突。

Empirical evaluations on numerical benchmarks and a 5G/6G OFDM system support the effectiveness of the method. Statistically, \textcolor{red}{DA-DGP attains the lowest robust quality loss among all one-shot Gaussian process surrogate variants and the highest AHP-TOPSIS closeness across the thirteen surrogate- and BO-based methods examined; without additional sequential simulator calls, its $\mathrm{HV}_{\mathrm{n}}$ remains essentially tied with the BO competitor that attains the highest hypervolume (qNEHVI) and exceeds the other BO baselines.}% [#8/#9/#11] In the engineering application, \textcolor{red}{the optimized parameters move the link toward the high-throughput shoulder of the Pareto front and produce selected-solution EVM in the same high-quality range as the sequential BO baselines, together with a tightly bounded selected-solution EVM spread in the physical-layer check.}% [#9/#11] These findings indicate that the framework can mitigate long-tail performance risks and enhance link robustness, offering a promising path for intelligent parameter optimization in complex manufacturing and communication processes.
A limitation of the current work lies in the reliance on the preset kernel bandwidth $\sigma_{k}$. Future research will focus on incorporating $\sigma_{k}$ directly into the variational inference framework. Realizing the adaptive learning of this hyperparameter based on data distribution characteristics will further improve the automation and generalizability of the model.

数值基准和 5G/6G OFDM 系统上的实证评估支持该方法的有效性。从统计角度看，\textcolor{red}{在所有一次性高斯过程代理变体中 DA-DGP 取得最低的鲁棒质量损失，并在十三种基于代理建模与 BO 的方法中取得最高的 AHP-TOPSIS 接近度；在无需额外序贯调用仿真器的情况下，其 $\mathrm{HV}_{\mathrm{n}}$ 与超体积最高的 BO 方法（qNEHVI）基本持平，并优于其他 BO 基线。}% [#8/#9/#11] 在工程应用中，\textcolor{red}{优化参数把链路推向 Pareto 前沿的高吞吐肩部，并给出与序贯式 BO 基线处于同一优良区间的所选最优解 EVM，同时在物理层检查中保持较小的所选最优解 EVM 扩散。}% [#9/#11] 这些发现表明，该框架能够缓解长尾性能风险并增强链路鲁棒性，为复杂制造与通信过程中的智能参数优化提供了有前景的路径。当前工作的一个局限在于依赖预设 kernel bandwidth $\sigma_{k}$。未来研究将聚焦于将 $\sigma_{k}$ 直接纳入 variational inference 框架。基于数据分布特征实现该超参数的自适应学习，将进一步提升模型的自动化程度和泛化能力。




\if0\blind
\begingroup
\section*{Data and Code Availability}
The code and data are available via the link https://zenodo.org/records/18263984.
%\vspace{-2em}
%\section*{Acknowledgements}
%This work was supported by the National Natural Science Foundation of China [Grant numbers 72301146, 72571142, 72561021, 72501133]; the China Postdoctoral Science Foundation (Grant number 2024T170430).
\endgroup
\fi

\bibliographystyle{apalike}
%\bibliographystyle{Chicago}
\spacingset{1}
\bibliography{IISE-Trans}



	
\end{document}
